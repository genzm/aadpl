# 拡張候補：四つの検討

この言語に足りていないもののうち、**検査可能性を落とさずに実現できる**と判断した四つを解説する。あわせて、実現できないと確定したものも記録する。

判断の基準は「何が書けるようになるか」ではなく、**「書けるようになったものについて、決定的な検査が保てるか」**である。この基準を外すと、既存のツールを使えばよいという結論になる。

以下、独自の用語は使わない。前提知識は Python での数値計算の経験のみとする。

---

## 全体の位置

| | 需要 | 実装量 | 検査 | 破る原則 | 前提 |
|---|---|---|---|---|---|
| 1. 不規則な形 | **最大** | 中 | 保てる | なし | なし |
| 2. 上限付きの反復 | 中 | **小** | 保てる | なし（上限付きなら） | なし |
| 3. 不動点の微分 | 中 | 中 | 保てる | なし | 2 |
| 4. 記法の拡張 | 中 | 中 | 保てる | なし | 表層構文 |

四つとも「作業量を実行前に決める」という中心の原則を破らない。破るものは §5 に分けて記す。

---

# 1. 不規則な形（ragged）

## 何が書けないか

群ごとに観測数が違う階層モデルが書けない。

```
群1: 20観測
群2:  5観測
群3: 33観測
```

現状は最大長に揃えて余りをマスクで潰す。正しい答えは出るが、計算とメモリが最大長に比例する。群サイズの偏りが大きいと無駄が大きい。

同じ問題が、可変長の系列（自然言語、強化学習のエピソード、不等間隔の時系列）でも出る。

**統計の実務で最も頻繁に当たる制約である。** 群サイズが揃っている階層モデルのほうが例外的だからである。

## なぜ書けないか

配列の形を実行前に決める、という制約による（設計文書 §4）。形が実行時にしか決まらないと、微分の逆向き計算を実行前に構成できない。

**ただし、この制約は「形の静的性」であって「変数集合の固定」ではない。** 群サイズが不揃いでも、確率変数の集合は $\{\gamma, \tau, a_1, \dots, a_G\}$ で固定されている。したがって中心の制約には抵触していない。

**抵触していないことが、この項目を最優先にする理由である。**

## どう実現するか

観測を一本の配列に平坦化し、群の境界を別の配列で持つ。

```
値:       [y₁ y₂ ... y₂₀ | y₂₁ ... y₂₅ | y₂₆ ... y₅₈]
オフセット: [0, 20, 25, 58]
```

形は $(58,)$ と $(4,)$ で、どちらも実行前に決まる。**不規則さが、形ではなく中身に移る。**

群ごとの集約は「オフセットに従って足し込む」操作になる。これは前に整理した添字写像の一種で、対応が不規則なだけである。

## 検査は保てるか

保てる。集約の逆向き計算は、既存の一本の規則（読み出しの逆は書き込みと足し込み）から出る。したがって内積の恒等式がそのまま適用でき、決定的に検査できる。

$$\langle \text{集約}(a),\ b\rangle = \langle a,\ \text{展開}(b)\rangle$$

## 確認すべきこと

実装前に、紙の上で三つ確かめる。

1. オフセット付き平坦配列の集約が、既存の添字写像の枠で書けるか
2. その逆向きが、既存の一本の規則で出るか
3. 群ごとの並列（rank）と、群内の不規則さが同居できるか

三つとも通れば、新しい原理を足さずに入る。通らない箇所があれば、そこが設計の修正点になる。

**この確認手順は、段階12で棄却による台の制限を作る前に「要らない」と判明した、あの手順と同じである。**

---

# 2. 上限付きの反復

## 何が書けないか

「収束するまで繰り返す」がモデルの中に書けない。

現状、反復は二通りで扱っている。ステップ数が定数の逐次計算（段階14）と、外側のホスト言語のループ（学習ループ）である。前者は回数が固定、後者は微分される式の中に入れられない。

## なぜ書けないか

作業量を実行前に決める、という原則による。逆向き計算を構成するには、何ステップ分の中間状態を保存するかが実行前に分かっていなければならない。

## サーベイで判明したこと

**この制約は、この設計に固有ではない。**

JAX の `while_loop` は逆向き微分ができない。XLA がメモリ要求の静的な上界を必要とするためである。`fori_loop` も、始点と終点が具体値の場合のみ逆向き微分を許す。2025年の自動微分コンパイラ研究 DaCe AD も、break や continue のないループのみを対象とし、静的な反復空間を保証すると明記している。

**「逆向き微分を事前に構成する方式」に共通の制約であった。**

一方、メモリの問題自体は解決されている。Enzyme は動的サイズのループについてループ内でキャッシュを再確保する。反復回数が未知の場合の準最適なチェックポイント方式（online treeverse, Stumm & Walther 2010）も存在し、Optimistix が実装している。

したがって**動的な反復は技術的には可能**である。問題は次項に移る。

## 上限を必須にすれば原則を破らない

```
反復⟨上限: 1000⟩ 条件が成り立つ間:
    ...
上限に達したらエラー
```

「収束するまで、ただし1000回まで」は**有界**である。作業量が実行前に決まる。

**この形は既に前例がある。** 棄却による台の制限を、固定予算 $K$ 回とし、$K$ 回とも棄却したらエラーにする、という判断（設計文書 §12）と同じ構造である。黙って近似せず、覆えない範囲はエラーで拒否する。

## 段階を分ける

段階14の逐次計算と同じ分割を使う。

| | 内容 | 実装量 |
|---|---|---|
| 第一段 | 微分を拒否する反復 | **小**。評価器が回るだけ。変換は入口で拒否する一行 |
| 第二段 | 微分できる反復 | 中。上限分の中間状態を保存 |

**第一段だけでも用途がある。** 生成、シミュレーション、収束判定つきの前処理は微分を必要としない。そして第一段は §3 の土台になる。

## 検査は保てるか

保てる。上限が定数なので、作業量は静的に有界のまま。第二段の逆向き計算も、上限分の枠を確保すれば既存の方式で構成でき、内積の恒等式が適用できる。

## 停止性について

**「この反復は必ず止まる」を静的に判定することは、原理的に不可能である。** 連続分布を持つ確率プログラムの概収束停止性の判定は $\Pi^0_2$ 完全であり（Beutner & Ong）、停止問題より一段階難しい。

したがって上限による打ち切りは、実装の妥協ではなく**唯一の選択肢**である。

---

# 3. 不動点の微分

## 何が書けないか

内側に最適化を含むモデルが書けない。

- 二階の最適化（ハイパーパラメータの学習）
- 不動点まで反復する層
- 制約付き最適化を経由する変分族
- 経験ベイズ

現状は反復数を定数で渡している（段階13の最適化）。答えは出るが、反復数が結果に影響し、その影響を評価する手段がない。

## 何を使うか

反復の目的が不動点 $z^* = f(z^*)$ を求めることなら、**反復を微分しない**。陰関数定理を使う。

$$\frac{\partial z^*}{\partial \theta} = \left(I - \frac{\partial f}{\partial z}\right)^{-1}\frac{\partial f}{\partial \theta}$$

**何回回ったかと無関係に、収束点だけから勾配が出る。** 逆向きに辿る必要がない。

メモリ要求が反復回数に対して一定になる（自動微分では線形に増える）。

実装は確立している。JAXopt は全ソルバで陰的微分に対応し、切り替え可能。Optimistix も陰関数定理による微分を既定にしている。

## なぜこの設計に合うか

三つ理由がある。

**線形方程式を解くのは固定作業量。** 中心の原則を破らない。

**検査が落ちない。** 三段構えで決定的に検査できる。

| 検査 | 内容 |
|---|---|
| 不動点条件 | $\|z^* - f(z^*)\|$ が閾値以下か |
| 線形方程式 | 解の残差が閾値以下か |
| 内積の恒等式 | 既存の枠がそのまま適用できる |

**既存の実装が正当化される。** 段階13で反復数を定数で渡している箇所が「不動点まで解く」に変わり、反復数が結果に影響しなくなる。

## 何が入らないか

不動点を求める反復にしか使えない。「軌道が折り返すまで」のような、不動点でない停止条件には効かない。

したがって適応的なサンプラは、これでは入らない。

---

# 4. 記法の拡張（マクロ層）

## 何ができないか

利用者が言語の中で機能を足せない。新しい変換や記法を足すには、実装言語で書くことになる。

## 構造

Racket の系譜に確立した設計がある（Ballantyne, King, Felleisen, OOPSLA 2020）。**固定された核と、その上のマクロ層**に分ける。

```
利用者が書く:  新しい記法
                  ↓ 展開
核の構成子:     Sample, 添字写像, 行列積, 逐次計算, ...
                  ↓
変換（微分・密度・推論）はここだけを見る
```

**決定的なのは、変換が核だけを扱うことである。** 利用者がマクロをいくつ足しても、微分の変換も密度の変換も一行も変わらない。展開後は核の構文木しか残らない。

拡張可能な DSL であっても、コンパイラは固定された核言語を中心に設計してよい、というのがこの構造の要点である。

## なぜこの設計が有利か

普通、拡張を許すと保証が壊れる。利用者の書いたものが正しい保証がないからである。

**しかしこの設計の保証は、変換を証明することではなく、出力を検査することから来ている。**

| | 保証の出所 |
|---|---|
| 証明ベースの手法 | 変換が一意で正しいことの証明 |
| Racket | 衛生性（変数の捕獲を防ぐ） |
| **この設計** | **出力が検査を通ること** |

したがって利用者のマクロに要求すべきは、正しさの証明ではなく、**出力が既存の検査を通ること**である。そして検査は全部揃っている。

| 検査 | 適用対象 |
|---|---|
| 構文検査 | 微分に関わる変換の出力が線形演算のみか |
| 内積の恒等式 | 逆向き計算を生成する変換 |
| 密度の数値積分 | 密度を生成する変換 |
| ビット一致 | 同値性を主張する変換 |
| 変数集合の一致 | モデルと近似分布を扱う変換 |

**利用者のマクロも、組み込みの機能も、同じ検査を通る。区別する理由がない。**

## 利用者が書けるもの

```
新しい分布:
  macro LogNormal(μ, σ) = Pushforward(exp, Normal(μ, σ))
  → 密度も台もサンプリング法も自動で導出される

モデルの部品:
  macro random_effect(name, τ) = name ~ Normal(0, τ) @ Group

新しい目的関数:
  macro beta_elbo(model, guide, β) = ...
  → 既存の密度評価を組み合わせるだけ。続けて微分できる

情報量規準:
  macro aic(model, slots, k) = -2 × (最大対数尤度) + 2k

記法の糖衣:
  macro dense(W, b, x) = ...
```

**全部、既にある意味の組み合わせに名前を付けているだけである。** 新しい意味を作っていない。だから安全で、だから検査がそのまま効く。

## 利用者が書けないもの

核に新しい構成子が要るものは書けない。核に新しい構成子を導入する必要がある場合、核の定義そのものを変更しなければならない（syntax-spec の既知の限界）。

| 拡張 | 誰が |
|---|---|
| 新しい分布、新しい目的関数、記法 | 利用者 |
| **サイトの五つ目の役割** | 設計者 |
| **新しい確率の構成子** | 設計者 |
| **新しい制御構造** | 設計者 |

**この境界は意味論的に正しい場所にある。** 四つの役割は統計学の構造（条件づけ・周辺化・最大化・近似）に対応しているので、五つ目を足すのは意味論の変更である。利用者に開くべきではない。

## 前提と、いま必要な判断

前提は表層構文（段階14）である。実装言語のデータ構造でモデルを組んでいる状態では、マクロを書く場所がない。

**そして段階14で一つ判断が要る。核の構文と糖衣の構文を分け、印字対象を核にすること。**

理由は三つ。

- 変換の出力は核なので、印字も核でないと読み込み直せない
- 利用者のマクロが増えても印字器を変えなくてよい
- 検査が核に対して定義される

**実装コストはほぼゼロだが、後から変えるのは高くつく。** いま構文を設計中であり、判断すべき時期である。

## 既知の弱点

マクロは展開されて消えるため、エラーが核の言葉で報告される。

```
利用者が書いた:  x ~ LogNormal(0, 1)
エラー:          「Pushforward の引数の型が…」
```

Racket でも実際に問題になっており、エラー報告と衛生性の研究が続いている領域である。**最初は諦め、後で対処するのが現実的である。**

---

# 5. 実現できないと確定したもの

曖昧に「いずれ」と書くと判断が先送りされ続けるため、確定として記録する。

## 再帰的な確率モデル・適応的なサンプラ

```
n ~ Poisson(5)
for i in 1..n:  z[i] ~ Normal(0,1)     ← 変数の数が実行時に決まる
```

**変数集合を実行前に固定する**という中心の制約に抵触する。外すには、モデルと近似分布の対応を実行前に検査する仕組みを捨てることになり、それは設計の根幹である。

適応的なサンプラ（NUTS）も同じ理由で入らない。**微分の問題ではない。** §2 と §3 で反復の問題を解いても、トレース構造が実行ごとに変わる問題は残る。

これは軽い損失ではない。事後サンプリングが要る場面では既存のツールを使うことになる。**「Stan が持つ推論も持たない推論も乗る」という言い方はできない。** 正確には「Stan が持たない推論（自由な変分推論の設計、検定、ベイズファクター）が、検査可能な形で乗る」である。

## 停止性の静的保証

$\Pi^0_2$ 完全であり、原理的に不可能。上限付きで回避する（§2）。

## 環境との相互作用・入出力

言語の外にある。ホスト言語側でやる。これは既存のフレームワークでも同じ分業であり、この設計に固有の制約ではない。

## 密度を不偏推定量で代用する方式

技術的には入るが、**採らない。** 対数を取ると不偏性が壊れて変分推論と噛み合わず、決定的検査が全て統計的検定に落ちる。得るものがなく、失うものが中心的である。

ただし将来入れられるよう、重みの扱いの規約だけ先に固定してある。

---

# 6. サーベイで消えた項目

検討したが不要と判明したものを記録する。同じ調査を繰り返さないためである。

## MAP 推定 — 既に書ける

「密度を勘定に入れつつ最大化する」役割が無い、と一度判断したが誤りだった。

$$\hat\theta = \arg\max_\theta\left[\log L(y|\theta) + \log \pi(\theta)\right]$$

括弧の中は「条件づけ」が生成する式そのものである。それを最大化するのは**言語の外側のループ**の仕事であり、役割ではない。

**役割が決めるのは式の形だけで、その式をどう使うかは言語の外。** この区別を混同していた。役割は四つで足りている。

なお、この整理から**「最大化する」という役割名は実態と合っていない**ことが分かる。実際にやっているのは「値を外から受け取り、密度を勘定しない」だけである。名前の変更を検討する価値がある。

## コードを値として扱う型 — 不要

多段階計算の枠組み（MetaML、MetaOCaml）を検討したが、**この用途には使えない。**

これらは純粋に生成的であり、生成されたコードはブラックボックスとして扱われ、中身を調べることができない。組み立てることはできるが、分解することはできない。

この設計の変換はモデルを**分解する**必要がある（サンプル節を見つけ、分布を読み、置き換える）。生成専用の機構では足りない。

コードの分解は内包的解析と呼ばれ、別の系譜（Mœbius, POPL 2022 など）で研究されているが、線形型との組み合わせが未解決と明記されており、この設計には直接使えない。

**マクロ層（§4）で足りるため、この方向は追わない。**

---

# 7. 進め方

```
1. 不規則な形         紙で三点を確認 → 実装
2. 上限付きの反復      第一段（微分しない版）のみ先に
3. 不動点の微分        2 の上に
4. マクロ層           表層構文の後
```

**そして段階14で今すぐ決めるべきこと：核の構文と糖衣を分け、印字対象を核にする。**

1 は需要が最も高く、中心制約に抵触しない。2 は実装量が小さく、3 の土台になる。3 は既存の実装が正当化される。4 は構文が前提。

四つとも検査可能性を落とさず、新しい原理をほとんど足さずに入る見込みがある。
