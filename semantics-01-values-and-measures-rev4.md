# 意味論 第1枚：値・型・接空間・測度（改訂第4稿）

**状態**: 草稿 / 議論用
**位置づけ**: 言語仕様（意味論）。実装計画ではない。

**第3稿からの変更**: trace 型を導入（§6.2）/ 潜在変数の消去を構成子として定義し、最大化は意味論の外と決定（§6.4, §10.9）/ 周辺化の戦略を分離（§6.5）/ 有界作業量の原則を新設（§7）/ VAE と階層モデルを紙の上で書いて見つかった穴を反映

---

## 0. この文書の役割

| | 対象 | 原則 |
|---|---|---|
| **意味論** | 何を計算しているか | 数学的自然さを妥協しない |
| **実装** | どう計算するか | 意味論の限定された断片。性能優先 |

実装が意味論の全体を覆う必要はない。覆っていない部分は**エラーとして拒否する**。§8 にその境界を書く。§10 に「検討したが採用しなかった選択肢」を理由付きで記録する。

**検証状況**: MNIST 上の VAE と、階層ロジスティック回帰（尤度比検定・ベイズファクター）を紙の上で構成済み。

---

## 1. 設計上の四つの決定

**(D1) 値の型と接空間の型を区別する。**
勾配は元の値と同じ型ではない。同一視は標準内積による選択であって、定義ではない。

**(D2) 測度が第一級であり、密度は導出物である。**
密度は参照測度に対する Radon–Nikodym 微分としてのみ意味を持つ。

**(D3) 分布は代数である。属性を手で持たせない。**
参照測度・台・制約変換・再パラメータ化可否・密度は、すべて構成から導出する。例外は「導出不可能な選択」と「構成に付随する証明義務」のみ。

**(D4) 将来の一般化のための継ぎ目を、コストがゼロのときに限り作る。**

**繰り返し現れる構造**: (D3) の帰結として、以下が三度同じ形をとる。

$$
\underbrace{\text{何が\emph{できる}か}}_{\text{構成から導出}}
\quad\neq\quad
\underbrace{\text{\emph{どう}やるか}}_{\text{戦略の選択}}
$$

適用箇所は勾配推定（§5.5）、`Restrict` のサンプラ（§5.6）、周辺化（§6.5）。

---

## 2. 値

### 2.1 型

```
基本型   b ::= R | Z | B | Fin n
型       τ ::= b | [s]τ | { l₁:τ₁, … } | M τ
```

### 2.2 配列は添字集合上の関数である

$I_s = \prod_i \mathrm{Fin}\,n_i$ に対し $⟦[s]\tau⟧ = (I_s \to ⟦\tau⟧)$。構造操作は添字写像 $\sigma$ による引き戻しとして一様に書ける（§4.5）。

### 2.3 レコード

ラベル付き積。パラメータ木（PyTree 相当）はこの積型そのもの。

**レコードへの写像は modifier として提供する。** 関数は構文的なオペランドとして渡され、値にはならない（§2.4 と両立）。パラメータの初期化がこれを要求する。

### 2.4 一階性

**関数は値ではない。** 二つの帰結を持つ。

**(a) 測度は代数の項（AST）として表現される。** したがって印字可能であり、再びパーサに入力できる。

**(b) 意味論の基盤が軽くなる。** 一階の確率プログラミング言語は、型を標準ボレル空間、純粋な項を可測関数、効果を持つ項を s-finite kernel と解釈すれば十分。quasi-Borel space は高階の場合にのみ必要であり、本言語では**不要**（§10.4）。

---

## 3. 接空間

### 3.1 定義

$$
\begin{aligned}
T(\mathtt{R}) &= \mathbb{R}, \qquad
T(\mathtt{Z}) = T(\mathtt{B}) = T(\mathrm{Fin}\,n) = \mathbf{0} \\
T([s]\tau) &= [s]\,T(\tau), \qquad
T(\{l_i:\tau_i\}) = \{l_i : T(\tau_i)\}, \qquad
T(M\,\tau) = \text{（未決 — §11.1）}
\end{aligned}
$$

$T$ は関手である。

### 3.2 帰結

**整数配列の接空間は零配列ではなく零空間である。**

> ❌ `∇Loss parameters` は parameters と同じ構成で勾配を返す
> ✅ `∇Loss parameters` は $T(\text{parameters の型})$ の元を返す

**$T$ は積を保存する。** これがレコード全体の勾配がレコードである理由。

### 3.3 公理

$T(\tau)$ は $\mathbb{R}$ 上のベクトル空間。逆伝播が使うのは加法・零元・スカラー倍のみ。**加法の存在が本質的**（§4.5 の押し出しが要求する）。

### 3.4 余接と勾配

$$\nabla f = g^{-1}(df), \qquad g : T(A) \xrightarrow{\ \sim\ } T^{*}(A)$$

既定の $g$ は標準内積。自然勾配法は $g$ を取り替えるだけになる（§11.1）。

---

## 4. 微分

### 4.1 プリミティブの許容クラス：PAP

**a.e. 微分可能性は合成について閉じていない**（Lee et al., NeurIPS 2020）。正しいクラスは **PAP**（piecewise analytic under analytic partition）であり、**intensional derivative** を持つ。これは至る所で定義され、連鎖律を満たし、ほとんど全ての入力で通常の微分と一致する。

**規定**: 本言語のプリミティブはすべて PAP 関数とする。これにより §8 の分岐セマンティクスは規約ではなく intensional derivative の計算として正当化される。

ωPAP spaces（LICS 2023）は枠組みを確率プログラムまで拡張し、trace 密度関数の a.e. 微分可能性と**密度計算に使える参照測度の存在**を証明している。後者は §5.1 の根拠。

### 4.2 逆モードの三分解と線形型

$$\text{前進モード AD} \;\to\; \text{unzip} \;\to\; \text{転置}$$

（You Only Linearize Once, POPL 2023）

**転置規則が必要なのは線形プリミティブだけである。** 非線形プリミティブには JVP 規則のみ。

**線形性は部分構造型システムで証明する。** コア IR は線形型を持つ中間言語（Linear A 相当）とし、前進モード AD の出力が証明可能に線形であること、証明可能に線形な関数がコスト・サイズ・線形性を保ったまま unzip・転置できることを保証する。

**checkpointing はこの分解の自由度である。** `let` 式を unzip する際の選択が checkpointing に対応する。後付けの機能ではない。

### 4.3 転置の根拠

$$(A \circ B)^{*} = B^{*} \circ A^{*}$$

### 4.4 高階微分

**forward-over-reverse** を既定とする。§4.2 の分解では前進モードが基礎なので自然に収まる。Fisher 情報・標準誤差・Wald 検定はここから出る。

### 4.5 添字写像の随伴（唯一の構造規則）

$$(\sigma_{*}g)(i) = \sum_{j \in \sigma^{-1}(i)} g(j)$$

| 演算 | $\sigma$ | 随伴 |
|---|---|---|
| broadcast | 定値写像 | 軸方向の和 |
| gather | 任意 | scatter-add |
| transpose / 並べ替え | 全単射 | 逆の並べ替え |
| reshape | 添字の同型 | 逆の reshape |
| filter | 包含 | 零埋め込み |
| rank 下の共有 | 対角 | セル間の和 |

**shape は転置の入力データであり、動的に持ち回ることは必須。** 上表の随伴はすべて単一の bulk 配列演算である（累積効果システムは不要、§10.2）。

### 4.6 rank との関係

$T(F^{\otimes}) = (TF)^{\otimes}$。**rank 専用の逆伝播規則は存在しない。**

---

## 5. 測度

### 5.1 参照測度

$$
\lambda_{\mathtt{R}} = \text{Lebesgue}, \quad
\lambda_{\mathtt{Z}} = \lambda_{\mathtt{B}} = \lambda_{\mathrm{Fin}\,n} = \text{数え上げ}, \quad
\lambda_{[s]\tau} = \bigotimes_{I_s} \lambda_\tau
$$

**参照測度を指定しない密度は定義されない。**

**部分多様体の規則**: 台が値の型より低次元の部分多様体になる場合、$\lambda_\tau$ に対する密度は存在しない。**測度はチャートからの押し出しとして構成し、値の型はチャートの次元を持つ。**

例：$J$ 成分の単体は $J-1$ 次元。値の型は `[J−1]R`、第 $J$ 成分は導出値。同じ規則が球面・正定値錐・直交行列にも適用される。

**チャートの選択は測度の選択である。** (D2) の帰結として、チャートは仕様の一部。

### 5.2 分布の代数

```
μ ::= Prim(name, args)
    | Pushforward(f, μ)        -- f は像への微分同相
    | Product(μ, ν)            -- 独立積
    | Restrict(μ, A, Z)        -- 事象 A への制限、Z = μ(A)
```

$$\texttt{Prim} = \{\ \mathrm{Uniform}[0,1),\quad \mathrm{Categorical}(w)\ \}$$

**`Product` はプログラムの層ではほぼ使われない。** 配列値のサンプリングは rank が担う（`?¨`）。`Product` は Gamma の構成のような分布代数の内部に留まる。

### 5.3 構成例

| 分布 | 構成 |
|---|---|
| Normal(0,1) | `Pushforward(Φ⁻¹, Uniform)` |
| Normal(μ,σ) | `Pushforward(x ↦ μ + σx, Normal(0,1))` |
| HalfNormal(σ) | `Restrict(Normal(0,σ), x>0, 1/2)` |
| 切り捨て正規 | `Restrict(Normal, [a,b], Φ(b)−Φ(a))` |
| Gamma | `Pushforward(f, Restrict(Product(Normal,Uniform), A, Z))` |
| Beta | 二つの Gamma の比 |
| Dirichlet | Gamma の正規化。値の型は `[J−1]R`（§5.1） |
| Bernoulli(p) | `Categorical(⟨1−p, p⟩)` |

### 5.4 導出される属性

| 属性 | Prim | Pushforward | Product | Restrict |
|---|---|---|---|---|
| 台 | 定義 | $f(S_\mu)$ | 積 | $S_\mu \cap A$ |
| 参照測度 | 定義 | 保存 | 積測度 | **不変** |
| 密度 | 定義 | $p_\mu(f^{-1}y)\lvert\det Df^{-1}\rvert$ | 積 | $p_\mu \mathbf{1}_A / Z$ |
| `cdf` | 定義 | 単調な $f$ なら合成 | — | 区間制限なら導出 |

**ヤコビアンは AD が計算する。**

**`cdf` は三つの用途を持つ。**

1. **原始分布としての適格性** — 逆変換法が可能か（§6.7 の共通乱数が要求）
2. **陰的再パラメータ化** — §5.7
3. **p 値と分位点** — 仮説検定、信頼区間

三つが独立に必要になって一致した。

### 5.5 パラメータ依存と勾配戦略

**適用可能性の判定（構文的、構成から導出）**:

| 条件 | pathwise |
|---|---|
| $\theta$ が `Pushforward` の $f$ にのみ現れ、下層が $\theta$ 非依存 | 可（明示的） |
| 原始分布の `cdf` が $\theta$ で微分可能 | 可（陰的、§5.7） |
| $\theta$ が `Restrict` の $A$ または $Z$ に現れる | 部分的（§5.6） |
| $\theta$ が `Categorical` の $w$ に現れる | **不可** |

離散分布が pathwise になり得ないことは、構成上そう書けないことから従う。

**選択**: 既定は pathwise、明示注釈で score function に切替可能。**戦略はサイトごとに決まる。** 当面の戦略は二つ。

`_reparam` は定義の展開にすぎない。**VAE では注釈が一切不要**であることを確認済み。

### 5.6 `Restrict` のサンプラと勾配

**固定予算 $K$ による棄却法**（サンプラは代数の一部ではなく戦略）。

1. $K$ 個の提案を独立に引く → 2. 受容判定 → 3. 最初の受容を選ぶ → 4. 全棄却ならエラー

- trace 構造が固定される（§8）
- 共通乱数が保たれる。アドレス = `(site, attempt k)`
- **$K$ 回の試行はランク1の配列演算である**
- 失敗確率は $K$ で制御

**勾配は既存の機構から落ちる。** 「連続な変換 + 確率的選択」なので §5.5 の戦略合成が適用される。Naesseth et al. (AISTATS 2017) の「再パラメータ化項 + 補正項」は、この合成の帰結。

**証明義務**: $Z$ は構成時に与える。§9.2 の統計的診断で検査する。

### 5.7 書き換え規則（数値的最適化）

| 規則 | 効果 |
|---|---|
| 陰的再パラメータ化 | `cdf` があるとき補正項が消え分散減少 |
| KL 閉形式 | 既知の組合せで MC を置換 |
| logsumexp、logsigmoid、softmax+CE の融合 | オーバーフロー回避 |

**性質（意味論の外）**: 非可逆（適用は一方向のみ）、非合成的（部分式の最適化を繋げても全体最適でない）、領域依存。実装の層の性質であり意味論の主張ではない（§10.6）。

---

## 6. 確率プログラム

### 6.1 分布とプログラムの層の区別

- **分布**（§5）— 密度が閉形式で得られる
- **プログラム**（本節）— `?` の連鎖。**出力の密度は一般に得られないが、trace 上の密度は得られる**

この非対称性が、`?` と `Score` が基本操作である理由。

**混合はプログラムとして書く。** 代数の構成子にはしない（§10.3）。

### 6.2 アドレスと trace 型

**アドレス**

$$a = (\text{呼び出しスタック},\ \text{サイトID},\ \text{添字パス})$$

三要素とも構文上の位置と rank の添字のみから決まり、$\theta$ にも乱数値にも依存しない。文字列ではなく構造化データ（整数タプル）。`Restrict` のサンプラは添字パスに試行番号を追加する。

**trace 型**

プログラムの型は、返り値の型に加えて trace 空間の型を持つ。

$$P : \tau_{\text{in}} \to G_{\mathcal{T}}\,\tau_{\text{out}}$$

$\mathcal{T}$ は潜在変数の構造（ラベル付き積）であり、アドレスはこの構造の中の位置として決まる。

$$\mathcal{T}_P = \prod_{a \in A_P} \tau_a, \qquad \lambda_{\mathcal{T}_P} = \bigotimes_{a \in A_P} \lambda_{\tau_a}$$

**trace 型は三つの役割を持つ。**

| 役割 | 効果 |
|---|---|
| trace 構造の静的固定 | §8 の制限の正当化（Trace types, POPL 2020） |
| **プログラム間のアドレス対応** | モデルとガイド、モデルと提案分布の整合。**文字列名が不要になる** |
| **部分 trace の指定** | §6.4 の潜在変数の消去。「どの部分に何を適用するか」 |

**二つのプログラムのアドレスは、同じ trace 型を持つときにのみ対応する。** これが VAE の ELBO を書けるようにする。

### 6.3 プログラムの意味

$$⟦P⟧ = (\mathcal{T}_P,\ p,\ w), \qquad \mu(dt) = w(t)\,p(t)\,\lambda_{\mathcal{T}_P}(dt)$$

観測は `Score (logDensity d x)` であり、別の基本操作を必要としない。

**rank 下の `Score`**: rank $r$ の下で `Score` が呼ばれた場合、各セルの log 重みに加算し、rank を抜けるときにセル方向の和を取る。これは §4.5 の軸方向の和であり、専用の規則を必要としない。

**実装契約（D4 の継ぎ目）**:

- 実装は $w(t)$ を厳密に計算する
- **`_importance` が生成するプログラムは、正規化定数の推定値ではなく重みの配列そのものを返す。** 正規化・平均・比は通常の配列演算として書く
- 数値計算は log 空間で行うが、意味論上の定義は乗法的とする。log を取ってから非線形関数に通す形で推論を定義してはならない

この契約により、ベイズファクターが $\mathrm{logsumexp}(\log w_1) - \mathrm{logsumexp}(\log w_0)$ という通常の配列演算になり、不偏推定量の比として well-defined になる。Stochastic Probabilities の機構を必要としない（§10.5）。

### 6.4 潜在変数の消去

trace 型の部分集合 $S \subseteq \mathcal{T}$ に対する操作を定める。

site の扱いは四分割する: **条件づける（観測）・積分する・最大化する・
trace に残す**。条件づけは `Score` / observed slot、積分は `Marginalize`、
最大化は意味論外の optimizer であり、残す site だけが出力 trace に残る。

**測度の操作（意味論の構成子）**

$$
\texttt{Marginalize}\ S\ P \qquad\qquad \texttt{Normalize}\ P
$$

- `Marginalize` — $S$ を積分して消す。trace 型が $\mathcal{T} \setminus S$ になる
- `Normalize` — 事後分布を構成する（`_importance` 等が実装）

加えて、部分密度

$$\texttt{LogDensity}\ S\ P\ t$$

は $S$ に属するアドレスの密度項のみを合計する。これにより最尤推定（事前分布を含めない）と MAP（含める）が同じ機構で書き分けられる。

**最大化は意味論の構成子にしない。**

最適化は `∇` と optimizer からなる通常のプログラムである（設計文書15章）。`Optimize` は modifier として提供する（§2.3 と同じ扱い、一階性は保たれる）。

理由は二つ。

1. **最大化は測度の操作ではない。** `Marginalize` と `Normalize` は測度から測度を作るが、$\arg\max$ は違う。(D2) と整合しない
2. **意味論に手続きが入り込まない。** 固定反復の最適化は「その手続きの出力」であり、宣言的な定義を持たない

**順序は字句的入れ子から自動的に出る。** $\int$ と $\max$ は可換でないが（$\int\max \neq \max\int$）、`Marginalize` が最適化される関数の**内側に書かれる**ので、順序は通常のプログラム構造が表現する。意味論に非可換性を書き込む必要がない。

（積分どうしは可換である。測度に基づく denotational semantics の可換性は証明されている。非可換なのは最大化との組み合わせのみ。）

**帰結：統計手法の対応**

| 手法 | 表現 |
|---|---|
| 最尤推定（GLMM） | `Optimize M {θ → LogDensity obs (Marginalize α (P θ))}` |
| MAP | `Optimize M {θ → LogDensity all (P θ)}` |
| ベイズ | `Normalize P`（`_importance` 等） |
| 変分推論 | `Optimize M {φ → ELBO P Q_φ}` |
| 経験ベイズ | 上の合成 |

**モデル記述は一つで足りる。** 頻度論とベイズの違いは、適用する操作の違いに帰着する。設計文書16章の主張の正確な形。

### 6.5 周辺化の戦略

(D3) の三度目の適用。**何を周辺化できるかは構成から決まり、どう周辺化するかは戦略の選択である。**

| 戦略 | 条件 | 保証 | 厳密密度 |
|---|---|---|---|
| 厳密列挙 | 台が有限 | **厳密** | 保つ |
| 共役性 | 構成が共役の形 | **厳密** | 保つ |
| 求積 | 低次元の連続変数 | 決定的・偏りあり | **保つ** |
| Monte Carlo | 常に | 不偏 | **失う** |

**求積が厳密密度を保つことの意味**（重要）:

$$
\begin{aligned}
\text{求積} &: \text{モデルを近似し、密度は厳密に保つ} \\
\text{MC 周辺化} &: \text{モデルを保ち、密度を近似する}
\end{aligned}
$$

求積で $\alpha$ を消したモデルは、**それ自体が厳密な密度を持つ well-defined な別のモデル**である。決定的な関数なので最適化でき、§9.1 の決定的テストも通る。GLMM の実務で求積が標準的なのはこの理由。

**したがって周辺化を戦略として上三つに限れば、Stochastic Probabilities は不要**（§10.5）。

**列挙と求積は計算の形が同じ**（潜在変数を固定サイズの軸に置き換え、重みつきで和を取る）。どちらも §4.5 の添字写像の枠に乗り、rank ネイティブで einsum に落ちる。ただし**誤差の性質が違うため、同じ構成子ではない**。

### 6.6 trace の伸縮（意味論としては定義する）

$$\mathcal{T}_P = \bigsqcup_{b} \mathcal{T}_b$$

各成分の参照測度が異なるため、成分をまたぐ密度比は次元差を埋める補正を要する。実装は §8 で固定構造に限定するが、**その制限は意味論の変更ではなく実装の断片性**である。

### 6.7 乱数と coupling

$$\texttt{random}(\text{runKey},\ \text{key}(a))$$

**キーは名前空間を持つ。**

$$\texttt{key} = (\text{名前空間},\ \text{パス})$$

名前空間は少なくとも `init`（パラメータ初期化）、`model`（`?` のアドレス）、`data`（ミニバッチのシャッフル）を含む。`?` を通らない乱数消費（初期化、dropout）は `model` 以外を使う。

**変換はキー引き継ぎ規則を伴う。**

$$\text{変換} = \text{AST 変換} + \text{アドレス写像} + \text{キー引き継ぎ規則}$$

**位置づけ**: 再現性の工夫ではなく **coupling** であり、勾配推定器の分散削減における第一級の構成要素。検定においても、帰無分布と対立分布の比較や手続き間の比較で分散を下げる。

**制約**: 原始分布は「アドレス1個につき消費する一様乱数の個数が固定」でなければならない。

---

## 7. 有界作業量の原則

実装は、すべての潜在的に無制限な作業を**静的に有界**にする。

| 箇所 | 有界化 | 節 |
|---|---|---|
| `Restrict` の棄却 | 固定予算 $K$ | §5.6 |
| 最適化ループ | 固定反復 $M$ | §6.4 |
| 離散の周辺化 | 台のサイズ | §6.5 |
| 連続の周辺化 | 求積節点数 $K$ | §6.5 |

これにより trace 構造が固定され（§8）、作業が rank の軸として一括実行できる。

**統計的にも損がない。** 固定反復の推定量は「その手続きの出力」として well-defined であり、すべての複製に同じ手続きを適用する限り検定の妥当性は保たれる。むしろ手続きが決定的になるため再現性が上がる。

**これは意味論上の制限ではなく、実装が採る一貫した戦略である。**

---

## 8. 実装が覆う範囲

| 意味論 | 実装 | 逸脱時 |
|---|---|---|
| trace 空間は非交和でよい | 単一の積（固定構造） | エラー：trace 構造が変化 |
| rank 下で各セルが任意の制御フロー | 全セル同一、または分岐ごとの分割実行 | エラー：分割不能 |
| `Restrict` は任意回の試行 | 固定予算 $K$ | エラー：$K$ 回とも棄却 |
| 最適化は収束まで | 固定反復 $M$ | — |
| 周辺化は任意の戦略 | 厳密列挙・共役性・求積のみ | MC 周辺化は未実装 |
| 一般の関手的変換 | 固定パイプライン | 事前条件違反でエラー |
| 任意の測度 | §5.2 の構成子で作れるもの | 未対応の構成子 |
| 再帰的な確率プログラム | 非再帰のみ | エラー |
| 重みは不偏推定でよい | 厳密計算 | — |
| ragged な潜在変数 | パディング + マスク | パディング位置もアドレスを持つ |

**この表が拡張の地図でもある。**

**trace 固定は妥協ではない。** Trace types（POPL 2020）は、確率プログラムが静的に既知のラベル集合でサンプルすることを型に取り込み、well-typed な推論プログラムが構成的に健全になる型システムを与えている。

**ragged について**: 潜在変数が群サイズに依存しない場合（階層モデルの $\alpha_g$ など）、ragged なのは観測軸のみであり `Score` はアドレスを持たないため、**trace 構造に影響しない**。パディング値は `Mask(x,m)` で安全値にしてから密度を評価し、密度セルも同じ `Mask` で零化する。単なる $0\times\log p(x)$ は $0\times(-\infty)=\mathrm{NaN}$ と逆伝播 residual の汚染を起こすため禁止する。

---

## 9. 診断

確率的プログラムは間違っていても動く。診断は付加機能ではなく、言語が使えるかどうかの条件である。

### 9.1 決定的テスト（厳密な一致を要求する）

厳密密度（§6.3）と共通乱数（§6.7）を採ったことで、以下が**統計的検定ではなく単体テスト**として書ける。**これが両者を採った主な実利である。**

| 検査 | 内容 |
|---|---|
| **随伴性（内積検査）** | $\langle L(v), u\rangle = \langle v, L^{*}(u)\rangle$。**AD 実装の最も強力な検査** |
| 密度の正規化 | 低次元で数値積分し 1 になるか |
| `_reparam` の同値性 | 同じ `runKey` で同じ値と同じ密度が出るか |
| 有限差分照合 | 共通乱数により $\theta$ 摂動で乱数が動かない |
| 参照測度・shape の伝播 | 構成木上で一貫しているか |

### 9.2 統計的テスト

**SBC**（Talts, Betancourt, Simpson, Vehtari, Gelman）を基盤とする。事前から $\theta$ を引き、データを生成し、推論を走らせ、順位統計量の一様性を検定する。

| 対象 | SBC が捕捉するもの |
|---|---|
| `Restrict` の $Z$（§5.6） | 密度が誤り、順位が非一様になる |
| 勾配推定量の不偏性 | 事後が偏る |
| `_importance` の重み | 同上 |

**制限**: 計算コストが高く、確率的検証なので低確率領域のエラーを見逃しうる。したがって **§9.1 を先に通し、§9.2 を後に置く**。

### 9.3 診断とユーザ機能は同じ計算形である

SBC、パラメトリックブートストラップ、検出力計算、シミュレーション研究は、**すべて「外側の複製 × 内側の手続き」という同じ形**をとる。

$$
\underbrace{N \text{ 複製}}_{\text{rank}} \times \underbrace{M \text{ 反復}}_{\text{fold}} \times \underbrace{K \text{ 節点}}_{\text{rank}}
$$

§7 の原則によりすべて静的に有界なので、完全にベクトル化される。**診断基盤への投資は、そのままユーザ向け機能になる。**

これは入れ子推論ではなく多軸の配列である。§10.5 の判断がここでも保たれる。

### 9.4 運用時の監視

ESS、重みの退化、勾配ノルム、ピークメモリ。

---

## 10. 採用しなかった選択肢

### 10.1 汎用の効果システム
**採らない。** 必要なのは §4.2 の線形性の追跡のみで、これは効果ではなく部分構造型で扱う。

### 10.2 Dex の Accum（累積）効果
**採らない。** Dex が結合的累積効果を必要とするのは pointful スタイルを採ったため。本言語の二層 IR では $\sigma_{*}$ が単一の bulk 配列演算に落ちる。

**再考条件**: コア IR を index comprehension まで分解する場合。

### 10.3 `Mixture` 構成子
**採らない。** 微分同相の押し出しでは作れず、独立構成子として足すと §5.4 の導出規則が壊れる（台が区間でなくなる、参照測度が食い違う、reparam 可否が単一の真偽値にならない）。

混合はプログラムとして書き、周辺化は §6.5 の戦略で行う。Tensor Variable Elimination（2019）が示す通り、列挙は離散変数ごとに軸を割り当てて broadcast で組み合わせ sum-product で潰す操作であり、§4.5 の枠にそのまま乗る。

### 10.4 quasi-Borel spaces
**採らない。** QBS は高階の確率プログラミング言語を解釈するために導入された。**本言語は一階である**（§2.4）ので、標準ボレル空間 + s-finite kernel で足りる。

**再考条件**: 関数を値にする場合。

### 10.5 Stochastic Probabilities
**採らない。** Lew et al. (PLDI 2023) は厳密密度を不偏推定に置き換えることで、`marginal` と `normalize` を第一級構成子にできる。

**理由**: 不偏性が保証されるのは密度であって対数密度ではない。$\mathbb{E}[\log\hat p] \neq \log\mathbb{E}[\hat p]$ なので、ELBO のように重みの対数を取る目的関数では不偏性が壊れる。IS・SMC・MH は助けるが **VI は助けない**。

副次的コスト:
- **§9.1 の決定的テストが全て統計的検定に落ちる**（最大の損失）
- 印字された推論プログラムを目で検算できなくなる（本言語固有）
- 入れ子で分散が乗算する

**周辺化を入れても不要である理由**: 求積は「モデルを近似して厳密密度を保つ」ため（§6.5）。ベイズファクターは重みの配列を露出させることで通常の配列演算になるため（§6.3）。事後予測チェックや SBC は入れ子推論ではなく多軸の rank であるため（§9.3）。

**継ぎ目は作る**（§6.3 の実装契約）。

**再考条件**: 密度が扱えない変分族、真に再帰的な入れ子推論、または **Monte Carlo による周辺化**が必要になるとき。

### 10.6 Herbie（浮動小数点の自動書き換え）
**統合しない。** ただしその知見（§5.7 の非可逆性・非合成性・領域依存性）は仕様に取り込む。特に「浮動小数点誤差は合成的でない」ことは、§4 の「変換は合成できる」という原則が数値の層では成り立たないことを意味する。

### 10.7 ADEV マクロの全体
**採らない（枠のみ）。** サイトごとの戦略という構造は採用するが、戦略は当面2つ。

### 10.8 GradInf の情報流型
**採らない。** coupling の発想は §6.7 に取り込むが、型システムは研究規模。

### 10.9 `Maximize` を意味論の構成子にする
**採らない。** MMAP（一部を積分し一部を最大化する問題）を第一級に扱う設計は存在するが、以下の理由で採らない。

- 最大化は測度の操作ではなく、(D2) と整合しない
- 意味論に手続き（固定反復）が入り込む
- 設計文書15章「学習は特別な言語機能にしない」と衝突する
- **順序（$\int\max \neq \max\int$）は字句的入れ子から自動的に出るため、意味論に書き込む必要がない**

最適化は `∇` と optimizer からなる通常のプログラムとし、`Optimize` は modifier として提供する。

**再考条件**: 離散的な決定変数に対する分枝限定など、最適化を意味論的に解析する必要が生じたとき（決定理論的な拡張）。現状の射程外。

---

## 11. 未決事項

### 11.1 $T(M\,\tau)$ の定義と自然勾配

**通常の VI（VAE を含む）には不要。** $\theta$ 依存は `_reparam` で関数側へ移る。必要になるのは自然勾配 VI のみ。

情報幾何では答えは確定している。指数型分布族なら Fisher 情報が対数正規化関数のヘッセ行列に帰着し、自然パラメータと期待値パラメータの Fisher 情報が互いに逆数であることから、一方での通常の勾配が他方での自然勾配を与える。

**問題**: §5.2 の代数は指数型分布族という構造を保持していない。$\mathrm{Normal} = \Phi^{-1}_*\mathrm{Uniform}$ からは自然パラメータが出てこない。

**見通し**: 自然勾配は $T(M\tau)$ を型として定義する重い解決ではなく、**§5.7 の書き換え規則の拡張**（構成をパターンマッチして指数型分布族と認識する）として実現できる可能性がある。KL 閉形式と同種の「認識の問題」。

### 11.2 特殊関数

不完全ガンマ関数・不完全ベータ関数とその微分。$\chi^2$・$t$・$F$ の cdf に必要で、仮説検定に不可欠。数値的に安定な実装は手間だが、**原始分布でも構成子でもないため後から追加できる**。

---

## 付録A：設計文書からの変更点

| 設計文書 | 本文書 |
|---|---|
| 2章：勾配は params と同じ構成 | §3.2：$T$ を適用した構成 |
| 7章：要素ごとに別の微分プログラム | §8：全セル同一制御フロー、または分割実行 |
| 8章：`?` と `Score` だけでよい | §6.1：層の分離の上で、その通り |
| 12章：`_expectGrad_` を全体に指定 | §5.5：構成から適用可能性が決まり、戦略はサイトごと |
| 15章：学習は特別な言語機能にしない | §6.4・§10.9：`Maximize` を構成子にしない根拠として採用 |
| 16章：同じ部品の組み合わせ | §6.4：trace 型の部分集合への操作の違いとして定式化 |
| 17章：shape 履歴は内部情報 | §4.5：随伴の入力データ。必須 |
| 19章：効果システムを作らない | §10.1：線形性は部分構造型で追跡 |
| 19章：checkpointing は後回し | §4.2：unzip の自由度として最初から表現できる |
| 19章：測度論的 PPL は対象外 | §6.6・§8：意味論では定義し、実装で限定 |

## 付録B：参考文献

**微分**
- Lee, Yu, Rival, Yang. *On Correctness of AD for Non-Differentiable Functions.* NeurIPS 2020. — PAP、intensional derivative（§4.1）
- Huot, Lew, Mansinghka, Staton. *ωPAP Spaces.* LICS 2023. — 確率プログラムへの拡張、基底測度の存在（§4.1, §5.1）
- Radul, Paszke, Frostig, Johnson, Maclaurin. *You Only Linearize Once.* POPL 2023. — 三分解、線形型、checkpointing（§4.2）
- Paszke et al. *Getting to the Point.* ICFP 2021 (Dex). — 添字集合と AD（§10.2）

**確率**
- Lew, Cusumano-Towner, Sherman, Carbin, Mansinghka. *Trace Types.* POPL 2020. — trace 型（§6.2, §8）
- Obermeyer et al. *Tensor Variable Elimination for Plated Factor Graphs.* 2019. — 列挙の配列的実装（§6.5, §10.3）
- Naesseth, Ruiz, Linderman, Blei. *Reparameterization Gradients through Acceptance-Rejection Sampling.* AISTATS 2017. — 棄却法の勾配分解（§5.6）
- Figurnov, Mohamed, Mnih. *Implicit Reparameterization Gradients.* NeurIPS 2018. — cdf による陰的再パラメータ化（§5.7）
- Lew, Huot, Staton, Mansinghka. *ADEV.* POPL 2023. — サイトごとの勾配戦略（§5.5, §10.7）
- Lew, Ghavamizadeh, Rinard, Mansinghka. *Stochastic Probabilities.* PLDI 2023. — `marginal`/`normalize` の第一級化と不偏密度推定（§6.4, §10.5）
- Becker, Lew et al. *Programmable Variational Inference.* PLDI 2024. — 厳密密度 + ADEV の VI パイプライン（§10.5）
- Becker, Huot et al. *Vectorized Programmable Inference.* POPL 2026 (GenJAX). — vmap と trace。最近傍の既存研究
- Arya, Huot, Schauer, Lew, Saad. *GradInf.* PLDI 2026. — coupling と factorization（§6.7）
- Staton et al. — 測度意味論の可換性（§6.4 の積分の可換性）

**診断・数値**
- Talts, Betancourt, Simpson, Vehtari, Gelman. *Validating Bayesian Inference Algorithms with SBC.* 2018.（§9.2）
- Modrák et al. *SBC Checking: The Choice of Test Quantities Shapes Sensitivity.* 2023.（§9.2）
- Panchekha, Sanchez-Stern, Wilcox, Tatlock. *Automatically Improving Accuracy for Floating Point Expressions.* PLDI 2015 (Herbie).（§5.7, §10.6）
