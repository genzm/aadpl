# 拡張候補：確認結果と検討

この言語に足りていないもののうち、**検査可能性を落とさずに実現できる**ものを整理する。

判断の基準は「何が書けるようになるか」ではなく、**「書けるようになったものについて、決定的な検査が保てるか」**である。この基準を外すと、既存のツールを使えばよいという結論になる。

以下、独自の用語は使わない。

---

## 0. 実装確認の結果

検討にあたって五点を確認した。結果として、**予想より状況が良い項目が二つ、作業が具体化した項目が一つある。**

| 確認 | 結果 |
|---|---|
| 式を保持して繰り返し評価できるか | **通る** |
| 部分的な変更に対する密度の差分が取れるか | **ない**。全体を再評価する |
| 不規則な集約が既存の枠に乗るか | **原理は通る**。実装に小さな穴が二つ |
| 分布を組み立てる機構がどこにあるか | 核にはある。定義は実装言語側 |
| 正規分布の密度は特化しているか | **特化していない**。構成から導出している |

以下、各項目でこの結果を踏まえる。

---

# 1. 不規則な形（ragged）

## 何が書けないか

群ごとに観測数が違う階層モデル。可変長の系列（自然言語、強化学習のエピソード、不等間隔の時系列）。

現状は最大長に揃えて余りをマスクで潰す。正しい答えは出るが、計算とメモリが最大長に比例する。

**統計の実務で最も頻繁に当たる制約である。** 群サイズが揃っている階層モデルのほうが例外的だからである。

## 中心の制約には抵触しない

配列の形を実行前に決める、という制約による。ただしこれは「形の静的性」であって「確率変数の集合の固定」ではない。群サイズが不揃いでも、確率変数の集合は $\{\gamma, \tau, a_1, \dots, a_G\}$ で固定されている。

**抵触していないことが、この項目を最優先にする理由である。**

## 確認の結果：原理は通る

観測を一本の配列に平坦化し、群の所属を別の配列で持つ。

```
値:      [y₁ y₂ ... y₅₈]      形 (58,)
群ID:    [0 0 ... 1 1 ... 2]   形 (58,)
```

形はどちらも実行前に決まる。**不規則さが、形ではなく中身に移る。**

群ごとの集約は、既存の構成子で表現できることが確認された。動的な添字に従って書き込み・足し込みを行う操作が既にあり、その逆向きは既存の一本の規則（読み出しの逆は書き込みと足し込み）で出る。内積の恒等式による検査も既に通っている。

群軸を枠にすれば、群ごとの並列と群内の不規則さは同居できる。

**新しい微分の原理は要らない。**

## 確認の結果：作業は二つ

原理が通る一方で、実装には具体的な不足が二点ある。

**(a) 逆向き規則を一本足す。**

現在、動的添字による足し込みは、別の操作の逆向きを生成するための内部的な部品として使われている。利用者の式に直接現れた場合の逆向き規則がまだない。

$$\text{足し込みの逆} = \text{読み出し}$$

**既にある対の、反対向きを足すだけである。** 小さいが、これが無いと利用者が書いた集約を微分できない。

**(b) 集約を厚い演算として足す。**

現在の素朴な経路は、群数 $G$ と観測数 $N$ に対して $G \times N$ の中間配列を作ってから潰す。メモリが $O(GN)$ になり、群数が多いと実用にならない。

$$\text{集約}: [N] \times [N] \to [G]$$

を一つの演算として加えるのが自然である。**意味論も微分規則も既存のものなので、性能のための追加であって原理の追加ではない。**

## 判定

> **入る。作業は逆向き規則を一本と、厚い演算を一つ。**

紙の上での検討段階は終わっている。

---

# 2. 上限付きの反復

## 何が書けないか

「収束するまで繰り返す」が、モデル記述と生成される式の中に書けない。

現状、反復は二通りで扱っている。ステップ数が定数の逐次計算（段階14）と、実装言語側のループ（学習の反復）である。前者は回数が固定、後者は微分される式の中に入れられない。

## この制約は一般的なものである

JAX の条件付きループは逆向き微分ができない。実行基盤がメモリ要求の静的な上界を必要とするためである。回数指定のループも、始点と終点が具体値の場合のみ逆向き微分を許す。2025年の自動微分コンパイラ研究も、break や continue のないループのみを対象とし、静的な反復空間を保証すると明記している。

**「逆向き微分を事前に構成する方式」に共通の制約であった。**

一方、メモリの問題自体は解決されている。Enzyme は動的サイズのループでキャッシュを実行時に再確保する。反復回数が未知の場合の準最適なチェックポイント方式（online treeverse）も存在し、Optimistix が実装している。

したがって**技術的には可能**である。

## 上限を必須にすれば原則を破らない

```
反復⟨上限: 1000⟩ 条件が成り立つ間:
    ...
上限に達したらエラー
```

「収束するまで、ただし1000回まで」は**有界**である。作業量が実行前に決まる。

**この形には前例がある。** 棄却による台の制限を固定予算 $K$ 回とし、$K$ 回とも棄却したらエラーにする、という判断と同じ構造である。黙って近似せず、覆えない範囲はエラーで拒否する。

## 段階を分ける

段階14の逐次計算と同じ分割を使う。

| | 内容 | 実装量 |
|---|---|---|
| 第一段 | 微分を拒否する反復 | **小**。評価器が回るだけ |
| 第二段 | 微分できる反復 | 中。上限分の中間状態を保存 |

**第一段だけでも用途がある。** 生成、シミュレーション、収束判定つきの前処理は微分を必要としない。そして第一段は §3 の土台になる。

## 停止性について

**「この反復は必ず止まる」を静的に判定することは、原理的に不可能である。** 連続分布を持つ確率プログラムの概収束停止性の判定は $\Pi^0_2$ 完全であり、停止問題より一段階難しい。

したがって上限による打ち切りは、実装の妥協ではなく**唯一の選択肢**である。

---

# 3. 不動点の微分

## 何が書けないか

内側に最適化を含むモデル。二階の最適化、不動点まで反復する層、制約付き最適化を経由する変分族、経験ベイズ。

現状は反復数を定数で渡している。答えは出るが、反復数が結果に影響し、その影響を評価する手段がない。

## 何を使うか

反復の目的が不動点 $z^* = f(z^*)$ を求めることなら、**反復を微分しない**。

$$\frac{\partial z^*}{\partial \theta} = \left(I - \frac{\partial f}{\partial z}\right)^{-1}\frac{\partial f}{\partial \theta}$$

**何回回ったかと無関係に、収束点だけから勾配が出る。** 逆向きに辿る必要がない。メモリ要求が反復回数に対して一定になる。

実装は確立している（JAXopt、Optimistix）。

## なぜこの設計に合うか

**線形方程式を解くのは固定作業量。** 中心の原則を破らない。

**検査が三段構えで残る。**

| 検査 | 内容 |
|---|---|
| 不動点条件 | $\|z^* - f(z^*)\|$ が閾値以下か |
| 線形方程式 | 解の残差が閾値以下か |
| 内積の恒等式 | 既存の枠がそのまま適用できる |

**既存の実装が正当化される。** 段階13で反復数を定数で渡している箇所が「不動点まで解く」に変わり、反復数が結果に影響しなくなる。

## 何が入らないか

不動点を求める反復にしか使えない。「軌道が折り返すまで」のような停止条件には効かない。

---

# 4. 記法の拡張（マクロ層）

## 確認の結果：意味論は既にある

分布の代数は核に実装されている。一様分布、有限カテゴリ分布、押し出し、直積が構成子として存在し、正規分布は一様分布の押し出しとして、対数正規分布は正規分布の押し出しとして定義されている。

**新しい分布を作るのに、新しい構成子は要らない。** 既存の組み合わせで書ける。

足りないのは表層だけである。分布を言語内の値として名前付けし、利用者が

```
dist LogNormal(μ, σ) = pushforward(exp, Normal(μ, σ))
```

と書く機構がない。定義は実装言語の関数として書かれている。

> **核の分布代数は存在する。利用者による定義・名前付け・再利用が、マクロ層の対象である。**

## 構造

Racket の系譜に確立した設計がある（Ballantyne, King, Felleisen, OOPSLA 2020）。**固定された核と、その上のマクロ層**に分ける。

```
利用者が書く:  新しい記法
                  ↓ 展開
核の構成子:     サンプル節、添字写像、行列積、押し出し、逐次計算, ...
                  ↓
変換（微分・密度・推論）はここだけを見る
```

**決定的なのは、変換が核だけを扱うことである。** 利用者がマクロをいくつ足しても、微分の変換も密度の変換も一行も変わらない。展開後は核の構文木しか残らない。

## なぜこの設計が拡張に有利か

普通、拡張を許すと保証が壊れる。利用者の書いたものが正しい保証がないからである。

**しかしこの設計の保証は、変換を証明することではなく、出力を検査することから来ている。**

| | 保証の出所 |
|---|---|
| 証明ベースの手法 | 変換が一意で正しいことの証明 |
| Racket | 衛生性（変数の捕獲を防ぐ） |
| **この設計** | **出力が検査を通ること** |

したがって利用者のマクロに要求すべきは、正しさの証明ではなく、**出力が既存の検査を通ること**である。そして検査は全部揃っている。

| 検査 | 適用対象 |
|---|---|
| 構文検査 | 微分に関わる変換の出力が線形演算のみか |
| 内積の恒等式 | 逆向き計算を生成する変換 |
| 密度の数値積分 | 密度を生成する変換 |
| ビット一致 | 同値性を主張する変換 |
| 変数集合の一致 | モデルと近似分布を扱う変換 |

**利用者のマクロも、組み込みの機能も、同じ検査を通る。区別する理由がない。**

## 利用者が書けるもの

```
新しい分布:
  dist LogNormal(μ, σ) = pushforward(exp, Normal(μ, σ))
  → 密度も台もサンプリング法も自動で導出される

モデルの部品:
  macro random_effect(name, τ) = name ~ Normal(0, τ) @ Group

新しい目的関数:
  macro beta_elbo(model, guide, β) = ...
  → 既存の密度評価を組み合わせるだけ。続けて微分できる

情報量規準:
  macro aic(model, slots, k) = -2 × (最大対数尤度) + 2k

記法の糖衣:
  macro dense(W, b, x) = ...
```

**全部、既にある意味の組み合わせに名前を付けているだけである。** 新しい意味を作っていない。だから安全で、検査がそのまま効く。

## 利用者が書けないもの

核に新しい構成子が要るものは書けない。

| 拡張 | 誰が |
|---|---|
| 新しい分布、新しい目的関数、記法 | 利用者 |
| **サイトの五つ目の役割** | 設計者 |
| **新しい確率の構成子** | 設計者 |
| **新しい制御構造** | 設計者 |

**この境界は意味論的に正しい場所にある。** 四つの役割は統計学の構造（条件づけ・周辺化・最大化・近似）に対応しているので、五つ目を足すのは意味論の変更である。

## 前提と、いま必要な判断

前提は表層構文である。実装言語のデータ構造でモデルを組んでいる状態では、マクロを書く場所がない。

**そして段階14で一つ判断が要る。核の構文と糖衣の構文を分け、印字対象を核にすること。**

- 変換の出力は核なので、印字も核でないと読み込み直せない
- 利用者のマクロが増えても印字器を変えなくてよい
- 検査が核に対して定義される

**実装コストはほぼゼロだが、後から変えるのは高くつく。** いま構文を設計中であり、判断すべき時期である。

## 既知の弱点

マクロは展開されて消えるため、エラーが核の言葉で報告される。

```
利用者が書いた:  x ~ LogNormal(0, 1)
エラー:          「押し出しの引数の型が…」
```

Racket でも実際に問題になっており、研究が続いている領域である。**最初は諦め、後で対処するのが現実的である。**

---

# 5. 撤回：サンプラは入る

**以前この文書は「動的に停止する反復が書けないので、適応的なサンプラは入らない」と記していた。これは誤りだった。**

## 混同していたこと

「動的である」を一つの性質として扱っていたが、実際には三つ別のものだった。

| 動的なもの | 抵触する制約 | 対処 |
|---|---|---|
| **サンプラの制御フロー** | **なし**（言語の外にある） | そのまま書ける |
| 反復回数 | 作業量の有界性 | 上限を置く（§2） |
| 確率変数の数 | 変数集合の固定 | §6 |

一行目が「なし」であることを見落としていた。木の深さが変わるのは**サンプラの制御フロー**であって、**モデルの変数集合ではない**。

## 確認の結果

式を一度構築し、変換を一度だけ走らせ、保持した束縛列と本体を環境だけ差し替えて再評価する経路が存在する。変換は評価のたびには走らない。

**これは学習の反復（Adam）が既に使っている仕組みである。** 同じ場所にサンプラを書ける。

```
logp  ← 全サイトを外から受け取る形の式        ← 一度だけ
grads ← その微分                              ← 一度だけ

for iter:                                     ← 実装言語のループ
    θ ← sampler_step(logp, grads, θ)          ← 動的な制御は自由
```

**ハミルトニアン型のサンプラは既に動いている。**

## 種類別の判定

| 手法 | 要るもの | 判定 |
|---|---|---|
| ランダムウォーク型 | 密度の評価 | **書ける** |
| ランジュバン型 | 密度と勾配 | **書ける** |
| ハミルトニアン型（固定ステップ） | 同上 | **実証済み** |
| 適応型（木の深さが動的） | 同上 | **書ける** |
| 並列焼き戻し | 温度付き密度 | 書ける |
| 逐次モンテカルロ（固定間隔） | 粒子軸と再標本化 | 書ける（粒子は枠に乗る） |
| 逐次モンテカルロ（適応的） | 上記＋動的判定 | §2 が要る |
| **成分ごとの更新** | **部分的な差分** | **書けるが遅い** |

**勾配に基づくサンプラは全部書ける。弱いのは成分ごとの更新である。**

## 成分ごとの更新について

確認の結果、部分的な差分を取る機構はない。評価は毎回、構文木を根から辿って全引数を評価する。依存グラフも、変更検出も、節点の記憶もない。

したがって、一つの変数だけを更新するサンプラは、更新のたびに全体の密度を再評価する。変数が1000個なら、一掃引で1000回の全体評価になる。

**手動で因子ごとに式を分けて保持すれば、影響する因子だけ評価できる。** 自動化するなら「因子分解＋自由変数の集合＋記憶」が新しい作業になる。

**優先度は低いと判断する。** 離散変数は列挙で消せるため、成分ごとの更新の需要自体が限定的である。

## 主張への含意

これで事後分布を得る手段が揃う。

$$\text{変分推論} + \text{求積} + \text{重点サンプリング} + \text{ハミルトニアン型サンプラ}$$

段階12で群数が少ないときに平均場近似が壊れた場面で、正しい答えを出す手段ができる。

一方で、**「推論をプログラムする」という主張の適用範囲は、目的関数の構成までである**ことは明記しておくべきである。目的関数を組み立てる部分は言語の中で変換として書かれ、それを消費するアルゴリズムは実装言語側にある。この構成は Stan（C++）、PyMC と NumPyro（Python）、Gen（Julia）と同じであり、この設計に固有の制約ではない。

---

# 6. 未確定：上限付きの可変個数モデル

```
n ~ Poisson(5)
z[1..n] ~ Normal(0,1)
```

上限 $N$ で打ち切れば、確率変数の集合は $\{n, z_1, \dots, z_N\}$ で静的に固定でき、どれが有効かをマスクが決める形になる。

$$\log p = \log p(n) + \sum_{i=1}^{N} \mathbb{1}[i \le n]\,\log p(z_i)$$

**しかし二つ未解決がある。**

**(a) 正規化が欠ける。** $n$ を打ち切ると $\sum_{n=0}^{N}\mathrm{Poisson}(n) < 1$ となり、裾を捨てた分だけ確率が足りない。厳密には正規化定数で割る必要がある。$N$ を大きく取れば無視できるが、「黙って近似しない」という方針からすると扱いを決めるべき箇所である。

**(b) 意味のないトレース要素。** $i > n$ の $z_i$ は値としては存在するが密度に寄与しない。これが検査（トレース構造の一致など）とどう噛み合うかは自明でない。

**§1（不規則な形）とは性質が違う。** そちらは形が不規則なだけで確率変数の数は固定だが、こちらは確率変数の数そのものが確率的である。同じ機構で済むと即断はできない。

判断は保留する。

---

# 7. 実現できないもの

## 上限のない可変個数モデル

無限に成長しうる木構造の生成モデルなど、打ち切りを置けないもの。**変数集合を実行前に固定する**という中心の制約に抵触する。

外すには、モデルと近似分布の対応を実行前に検査する仕組みを捨てることになり、それは設計の根幹である。

**ただし、統計の応用でこれが必要になる頻度は低い。**

## 停止性の静的保証

$\Pi^0_2$ 完全であり、原理的に不可能。上限付きで回避する（§2）。

## 環境との相互作用・入出力

言語の外にある。実装言語側でやる。既存のフレームワークでも同じ分業であり、この設計に固有の制約ではない。

## 密度を不偏推定量で代用する方式

技術的には入るが、**採らない。** 対数を取ると不偏性が壊れて変分推論と噛み合わず、決定的検査が全て統計的検定に落ちる。得るものがなく、失うものが中心的である。

ただし将来入れられるよう、重みの扱いの規約だけ先に固定してある。

---

# 8. 検討して消えた項目

同じ調査を繰り返さないために記録する。

## MAP 推定 — 既に書ける

「密度を勘定に入れつつ最大化する」役割が無い、と一度判断したが誤りだった。

$$\hat\theta = \arg\max_\theta\left[\log L(y|\theta) + \log \pi(\theta)\right]$$

括弧の中は「条件づけ」が生成する式そのものである。それを最大化するのは**実装言語側のループ**の仕事であり、役割ではない。

**役割が決めるのは式の形だけで、その式をどう使うかは言語の外。** 役割は四つで足りている。

なお、この整理から**「最大化する」という役割名は実態と合っていない**ことが分かる。実際にやっているのは「値を外から受け取り、密度を勘定しない」だけである。名前の変更を検討する価値がある。

## コードを値として扱う型 — 不要

多段階計算の枠組み（MetaML、MetaOCaml）を検討したが、**この用途には使えない。**

これらは純粋に生成的であり、生成されたコードはブラックボックスとして扱われ、中身を調べることができない。組み立てることはできるが、分解することはできない。

この設計の変換はモデルを**分解する**必要がある。生成専用の機構では足りない。

コードの分解は内包的解析と呼ばれ別の系譜（Mœbius, POPL 2022 など）で研究されているが、線形型との組み合わせが未解決と明記されており、この設計には直接使えない。**マクロ層（§4）で足りるため、この方向は追わない。**

## 正規分布の密度の特化 — そもそも特化していない

確認の結果、正規分布の対数密度に専用の分岐は存在しなかった。一様分布を分位関数で送り、さらに一次関数で送るという構成をそのまま辿り、押し出しの一般規則を適用してヤコビアンを順方向微分で計算している。

その結果が閉じた式と $10^{-12}$ で一致することを検査している。

**これは「特化が正しい」より強い状態である。** 分布の代数が実際に密度を生産していることの証拠になっている。文書の該当箇所は断定に書き換えた。

---

# 9. 進め方

```
1. 不規則な形         逆向き規則を一本 + 厚い演算を一つ
2. 上限付きの反復      第一段（微分しない版）のみ先に
3. 不動点の微分        2 の上に
4. マクロ層           表層構文の後
```

**そして段階14で今すぐ決めるべきこと：核の構文と糖衣を分け、印字対象を核にする。**

1 は需要が最も高く、中心制約に抵触せず、作業が具体化している。2 は実装量が小さく、3 の土台になる。3 は既存の実装が正当化される。4 は構文が前提。

四つとも検査可能性を落とさず、新しい原理を足さずに入る。

そして**サンプラは、これらを待たずに書ける**（§5）。優先順位の外にあり、いつでも着手できる。
