# この言語の設計と実装

要求から設計がどう決まり、何が実際に動いているかを一本にまとめる。前提知識は Python での数値計算の経験のみとし、標準的でない用語は使わない。

**この文書が置き換えるもの**: `design-rationale-plain.md`、`implementation-for-python-users.md`、`related-work-and-differences.md`、`from-requirements-to-design.md`。

---

## 1. 何を作ろうとしているか

統計モデルを一度だけ書けば、そこから勾配（最適化のため）、事後分布の近似（ベイズ推論のため）、検定統計量とその分布（頻度論の手続きのため）が得られる言語を作る。

現在の実務では、この三つは別々に書く。最適化には PyTorch で損失関数を、ベイズ推論には Stan でモデルブロックを、検定には scipy で統計量を書く。三つは同じ数理モデルを表しているはずだが、記述としては独立しており、互いに整合しているかを確かめる機械的な手段がない。事前分布を一箇所変えたとき、他の二つに正しく反映されているかは、人間が目で見て判断するしかない。

そこで要求を二つ立てる。

**要求A。** 単一のモデル記述から上の三つが生成でき、かつ生成物が正しいこと・互いに整合していることが、決定的なテストで確かめられる。ここで「決定的」とは、乱数や有意水準に依存せず、実行するたびに同じ合否が出るという意味である。

**要求B。** 言語は配列を基本の値とする。演算をどの次元に適用するかは構文で明示し、次元の解釈を暗黙の規則に委ねない。

要求Bは要求Aから出てくるものではなく、独立の判断である。根拠は、数理統計の記述の自然な単位が多次元配列であることと、numpy 系のコードで最も混入しやすい誤りが次元の暗黙の扱い（ブロードキャストの意図しない発動、軸の取り違え）だという観察にある。

---

## 2. 要求Aの前半だけでは設計は決まらない

「一つの記述から三つを生成する」だけなら、実現方法は複数あり、いずれも実在する。

**方法1：実行時に割り込む。** モデルを普通の関数として書き、その中の「サンプルする」という操作を、実行のたびに外から差し替える。サンプルを実際に乱数で引けばシミュレーションになり、値を固定して密度を積み上げれば尤度計算になる。Pyro がこの方式である。

**方法2：構文木を複数の方法で解釈する。** モデルの記述を構文木として保持し、それを読むインタプリタを複数用意する。密度インタプリタの出力は自動微分に対応した数値ライブラリの値なので、勾配はそのライブラリ（たとえば JAX）から得られる。NumPyro がこの方式である。

**方法3：すべてを一つの関数に集約する。** モデル記述をコンパイルして対数密度関数を作り、それを固定の推論エンジンに渡す。Stan がこの方式である。

三つとも動いている。したがって、生成できることだけを求めるなら、新しい言語を作る理由はない。

---

## 3. 決め手は検証の側にある

設計を分けるのは要求Aの後半、すなわち検証の要求である。

決定的なテストというものを具体的に考える。

- 微分の正しさを、内積の恒等式で確かめる。関数の線形近似を $L$、その随伴を $L^{*}$ とすると、任意のベクトル $v, u$ について $\langle L(v), u\rangle = \langle v, L^{*}(u)\rangle$ が厳密に成り立つはずである。乱数で $v, u$ を生成し、両辺が浮動小数点の丸めの範囲（$10^{-12}$ 程度）で一致することを要求する。近似の検査ではなく恒等式の検査なので、合否が決定的に決まる。
- モデルの書き換え（たとえば正規分布のサンプルを「標準正規のサンプルを一次変換したもの」に置き換える再パラメータ化）の前後で、同じ乱数の種に対して出力がビット単位で一致することを要求する。
- 生成された勾配計算の途中経過が、実際に線形な演算だけで構成されていることを、構成そのものを調べて確かめる。値ではなく構造の検査である。

これらに共通する前提がある。**検査の対象が、実行して消える途中経過ではなく、取り出して調べられる対象として存在しなければならない。**

方法1では成立しない。尤度の式は実行中に積み上げられて数になり、式そのものはどの時点にも存在しない。符号を一つ間違えた変分下界は、エラーを出さずに少し悪い数値を出すだけであり、構造を検査する対象がない。

方法2では、最終結果の数値を他の実装と突き合わせることはできる。しかし途中の各段——書き換え前後の同値性、線形近似の構造——を個別に検査できない。また微分を外部ライブラリに委ねているため、「そのライブラリと一致する」以上の保証が原理的に得られない。

方法3では、対数密度の正しさまでは検査できるが、推論の手続きが固定されているため、検定を同じ枠組みに載せられない。

そこで次の方針に至る。

> モデルの記述を構文木として保持し、勾配の導出も、推論の準備も、検定の構成も、すべて**構文木を受け取って構文木を返す関数**として実装する。

以下これを「書き換え」と呼ぶ。各段の出力がすべて具体的な構文木として手元に残るので、印字して目で読めるし、構造を検査するテストが書けるし、段と段を関数合成でつなげる。上に挙げた決定的テストは、すべてこの形式の帰結として書ける。

**一つ認めておくべきことがある。** この方針が優位なのは、途中経過を段ごとに検査することに価値を置く場合に限る。最終結果の数値が合っていれば十分だと考えるなら、方法2が最も合理的であり、新しい言語は不要である。この設計全体は「確率的なプログラムは間違っていても動いてしまうので、途中経過を検査できる形にしておく価値が高い」という一つの判断の上に立っている。

---

## 4. 方針が言語に課す制約

書き換えですべてを済ませると決めると、言語の側にいくつかの制約が生じる。いずれも表現力を削る決定である。

**関数を値として扱わない。** 関数を配列やレコードに格納したり、引数として渡したりできない。理由は、書き換えの対象である構文木が、印字可能で完結したデータでなければならないからである。関数が値になると、実行時に作られる関数（環境を抱えた閉包）が値の中に現れ、構文木として取り出せなくなる。副次的な利点として、確率的な言語の数学的な基礎付けが簡単になる。関数を値にしない範囲では、型を可測空間、プログラムを可測な変換として素直に解釈できるが、関数を値にするとこの解釈が破綻し、より重い理論装置が必要になる。

**確率変数の集合を実行前に固定する。** モデルがどの確率変数を持つかは記述から静的に決まるものとし、実行のたびに変数が増減するモデルは扱わない。これにより、モデルと近似分布の対応関係が実行前に検査でき、変数の取りこぼしが実行してから発覚するのではなく、書き換えの時点で拒否される。

**配列の形を実行前に決める。** 微分の書き換えでは「読み出しの向き」の演算を「書き込みと加算の向き」の演算に反転させる必要がある（§6）。反転後の演算を構成するには出力の形が既知でなければならず、形が実行時にしか決まらないと、書き換えが実行前に完結しなくなる。

**微分するプリミティブの関数クラスを規定する。** 各プリミティブが「ほとんど至る所微分可能」であるだけでは足りない。この性質は合成で保存されないことが知られており、各部品が良くてもプログラム全体が良い保証が得られない。合成で閉じたクラス（区分的に解析的な関数のクラス）にプリミティブを限定する。この規定は実装コストを伴わない。実装するプリミティブはすべてこのクラスに収まっているので、条件を明文化するだけでよい。$\max$ の折れ点のような微分不能点での値が、恣意的な取り決めではなく、規定されたクラスにおける計算結果として定まる。

**作業量を静的に有界にする。** 反復回数・節点数・試行回数はすべて実行前に定数として決まる。「収束するまで」は書けない。この制約は個別の理由で何度も現れた（求積の節点数、内側の最適化の反復数、逐次計算のステップ数）が、後から見るとすべて同じ一つの原則だった。

---

## 5. 微分の組み立て

勾配の導出は二段の書き換えとして実装する。

第一段は線形化である。プログラムと入力点を受け取り、その点における一次近似を表すプログラムを返す。非線形性はここですべて処理される。たとえば指数関数の一次近似は「入力点での値を係数とする掛け算」であり、係数は第一段の時点で計算済みの値になる。したがって第一段の出力は、線形な演算だけからなるプログラムである。

第二段は転置である。線形なプログラムを受け取り、その随伴、すなわちすべての演算を逆向きにしたプログラムを返す。入力が線形であることは第一段が保証しているので、第二段は線形代数の規則を機械的に適用するだけであり、判断を含まない。

二段に分ける利点は三つある。各段が単純になること（第一段は微積分だけ、第二段は線形代数だけ）。高階の微分が新しい仕組みなしに得られること（線形化を二度施してから転置する）。そして最も重要なのは、二段のあいだに検査点ができることである。第一段の出力が線形であることは構造として検査でき、第二段の出力は内積の恒等式で検査できる。一段で書くと、この中間の検査点が存在しない。

### 手書きは消えない

正確を期す。$\exp$ の微分が $\exp$ であること、$\log$ の微分が $1/x$ であることは、誰かが必ず手で書く。この設計でも 9 個程度のプリミティブについて手で書いており、そこは他の実装と変わらない。

減るのは二点だけである。

第一に、書く内容が「逆向きの計算」ではなく「順方向の一次近似」になる。教科書の微分そのものを書けばよく、勾配が逆から流れてくる文脈を考えなくてよい。**ただしこれは JAX と共通の性質であり、この設計に固有ではない。**

第二に、構造操作（転置・切り出し・複製・形の変更）の逆向き規則が一本に統合される（§6）。個別に書くと 4 つ以上になるものが 1 つになる。

手書き部分の検査手段は、他の実装と同じく数値微分しかない。差が出るのは機械が生成した部分で、そこは内積の恒等式で厳密に検査できる。要約すると、**手書きの面積を最小にし、残りを厳密に検査可能な機械生成に寄せる**設計である。

---

## 6. 配列の側

要求Bは別の系列の帰結をもたらし、しかも要求Aが残した問題を解決する。

### 構造の操作を一種類に統一する

配列を「添字から値への関数」とみなす。すると転置・切り出し・複製・形の変更は、すべて「新しい添字から古い添字への写像 $\sigma$ による読み出し」として統一的に表せる。

$$b(j) = a(\sigma(j))$$

この統一の配当は微分に現れる。$\sigma$ で読み出す操作の随伴は、**同じ写像で書き込んで重複分を加算する操作**であり、これが一本の規則になる。

具体例で確認する。長さ 2 の配列 $a = [5, 7]$ を 3 回複製して形 $(3,2)$ の $b$ を作ると、$b$ の各要素は $a$ の要素を読み出している。$a[0]$ は 3 箇所から読まれる。したがって $a[0]$ の勾配は、その 3 箇所の勾配の合計になる。結果として「複製した軸方向の和」になるが、**和を取ることを人間が判断していない**。「読んだ場所に足し込む」という一本の規則から出ている。

切り出しでも同じ規則が働く。読まれなかった場所には何も足されないので、勾配はゼロのまま残る。これが「切り出しの逆はゼロ埋め」と呼ばれるものの正体である。

実装上は、構文木に置くのを「$\sigma$ の記述」だけにし、読み出しと書き込みの二つのノードで表す。この二つは互いに相手の逆になっているため、二階以上の微分でも規則が破綻しない。

### 二種類の軸

配列指向のもう一つの柱は、演算をどの次元に適用するかを構文で明示することである。そして軸には性質の異なる二種類がある。

| 軸の種類 | 中身 | 扱い |
|---|---|---|
| **独立な軸** | 各要素が互いに無関係（バッチ、複製、群） | 一括して適用 |
| **依存する軸** | 各ステップが前のステップに依存（時刻、系列） | 逐次に適用 |

前者は「複製」の一種なので $\sigma$ の枠に乗り、微分も自動的に正しくなる。後者は乗らないので、別の構成子を用意した（§10 の Phase 14）。ただし後者もステップ数が静的に決まる（§4 の有界性）ので、原理は追加していない。

拡散モデルがこの対比の実例になる。サンプル 256 個は互いに無関係なので独立な軸、時刻 $T=100$ は前のステップに依存するので依存する軸である。

### 要素単位のループを言語から外す

利用者は配列全体への演算だけを書き、個々の要素を添字で辿るループは書けない。表現力の大きな制限だが、見返りも大きい。微分が生み出す逆向きの演算は、ループがなければすべて配列全体への一括演算（軸方向の和、散布加算、ゼロ埋め）に落ちる。ループ内での逐次的な書き込みを微分する際に必要になる特別な仕組み（これを解決するために効果システムを備えた研究言語がある）が、問題ごと消滅する。

### 演算の粒度を粗くする

中間表現の命令は、行列積や軸方向の縮約のような、一命令が大量の計算を含むものに限る。すると、構文木を辿りながらその場で実行する最も素朴な方式でも、実用的な速度が出る。一命令の裏で BLAS が数百万回の積和を行うなら、木を辿る事務コストは全体のごく一部にしかならない。

実測では、演算あたりの固定費が約 0.5 マイクロ秒、MNIST の 1 ステップが約 12 ミリ秒で、固定費の寄与は 0.1% 未満だった。

**ここで二つの要求が噛み合う。** 書き換えを中心に据える方式の弱点は速度である（数式処理システムが数値計算の実用に耐えないのはこのためである）。その弱点を、配列指向が要求する粗い演算粒度が結果として塞いでいる。狙って設計したのではなく、二つの独立な要求が同じ形に収束した。

---

## 7. 分布

### 少数の部品と組み立て規則で定義する

通常のライブラリでは、分布ごとにクラスを作り、密度・サンプリング・台などの属性をそれぞれ手で書く。分布が 40 個あれば属性は 200 個手書きされ、相互の整合性を保証するものはない。

この言語では、原始的な分布を一様分布と有限カテゴリ分布の二つだけとし、そこから「可測な関数で送る」「直積を取る」「台を制限する」の三つの組み立てで他の分布を作る。正規分布は原始ではなく、一様分布を正規分布の分位関数で送り、さらに一次関数で送ったものとして定義される。

密度・台・サンプリング法・再パラメータ化の可否は、すべて組み立ての木から機械的に導出される。たとえば関数で送った分布の密度は、変数変換の公式を木に沿って適用したものになる。Stan で手で書き足していたヤコビアンの補正は、この公式の一部としてここに含まれており、書き忘れる箇所がない。

**そして実装は、実際に導出のほうを使っている。** 正規分布の対数密度に専用の分岐は存在しない。一様分布を分位関数で送り、さらに一次関数で送るという構成をそのまま辿り、押し出しの一般規則

$$\log p(x) = \log p_{\text{base}}(f^{-1}(x)) + \log\left|\frac{d f^{-1}}{dx}\right|$$

を適用してヤコビアンを順方向微分で計算している。その結果が、教科書に載っている閉じた式と $10^{-12}$ 以内で一致することを検査している。

これは「閉じた式への特化が正しい」より強い状態である。**分布の代数が飾りではなく、実際に密度を生産している**ことの証拠になっている。

将来、性能上の必要が生じたときは、よく使う組み立てを認識して閉じた式に置き換える最適化を入れる余地がある。そのときも順序は固定する。定義は常に組み立てからの導出であり、閉じた式はそれとの一致を検査される最適化である。逆にはしない。閉じた式そのものを定義にすると、比較すべき基準が失われる。

### 混合分布は部品にしない

混合を組み立てに加えると、台が区間でなくなり、基準となる測度が成分間で食い違い、再パラメータ化の可否が単一の真偽で答えられなくなる。部品を一つ増やすために導出の仕組み全体を失う。

代わりに、混合は**プログラムとして書く**。離散変数を引いて分岐する形にし、その離散変数を消す（周辺化する）のは書き換えで行う。離散変数に配列の軸を一本割り当て、全ての場合を同時に計算して最後に和を取る。分岐が軸方向の一括計算になる。

**部品を減らして書き換えに仕事を移す**、というのがこの設計で繰り返し現れる型である。

### 再パラメータ化は定義の展開である

変分推論では、平均や分散で微分できるようにするため、$z \sim \mathcal{N}(\mu,\sigma^2)$ を $z = \mu + \sigma\varepsilon,\ \varepsilon \sim \mathcal{N}(0,1)$ と書き換える。Pyro では分布ごとに手書きされる別メソッドである。

この設計では新しい操作ではない。正規分布の定義の中に $x \mapsto \mu + \sigma x$ という関数が既に部品として入っており、書き換えはそれをプログラムの側に書き下すだけである。定義を展開しているだけで、新しい意味を与えていない。

だから検査ができる。書き換えの前後で、同じ乱数の鍵に対して同じ値・同じ密度が出るはずであり、ビット単位の一致を要求できる。新しい操作なら比較すべき基準がないので、この検査は書けない。

そして実際に配当があった。変分オートエンコーダを実装したとき、どのサンプル地点をどう微分するかの注釈を一つも書かなかった。判定が組み立ての木から自動的に出るため、**注釈のための機能自体が存在しない**。Pyro では利用者が指定する必要がある部分である。

---

## 8. 推論と検定

### サンプル節を目的別に書き換える

モデルの中の「サンプルする」という節は、目的に応じて書き換えられる。乱数を実際に引く形にすればシミュレータになり、与えられた値の密度を積み上げる形にすれば尤度計算になり、値を外から受け取る引数にすれば変分推論や検定が必要とする形になる。結果はいずれも普通のプログラムであり、印字して確認でき、続けて微分の書き換えにかけられる。

### サイトの四つの役割

より整理された形では、各サンプル地点に四つのうちどれかの役割を与える。

| 役割 | 何をするか | 使う場面 |
|---|---|---|
| 条件づけ | 値を固定する | 観測データ |
| 積分する | 和や求積で消す | 混合の離散変数、階層モデルの群効果 |
| 最大化する | 最適値を入れる | 尤度比検定の推定 |
| 残す | 引数のまま | 変分推論の潜在変数 |

この四つの指定だけで、変分推論も検定もベイズファクターも出る。実際、尤度比検定とベイズファクターの違いは、対象パラメータの役割を「最大化する」から「条件づける」に変える一語だけである。

$$\text{頻度論の } \max_\theta \quad\longleftrightarrow\quad \text{ベイズの } \int d\theta$$

統計学における二つの立場の対立が、一つの軸の上の二点になっている。

### 乱数を純粋関数にする

乱数は内部状態を持つ生成器ではなく、鍵と番号の組から値への決定的な関数として与える。この選択の主目的は検証である。

パラメータをわずかに動かして目的関数の差分を取り、勾配と突き合わせる検査を考える。生成器方式では二回の評価が異なる乱数列を消費するため、モンテカルロ法の分散（典型的に $10^{-1}$ のオーダー）が見たい差分（$10^{-6}$ のオーダー）を完全に覆い隠し、この検査は原理的に成立しない。鍵と番号の方式では、パラメータを動かしても各サンプル地点の乱数はビット単位で不変なので、差分にはパラメータの効果だけが残り、検査は決定的なテストになる。

同じ方式を JAX も採っているが、あちらの動機は並列化であり、こちらの動機は検証である。

---

## 9. 検証の全体構成

以上の選択が噛み合った結果として、テストは二層に組める。

**第一層は決定的なテスト。** 内積の恒等式、書き換え前後のビット単位の一致、書き換え結果の構造検査、低次元での密度の数値積分が 1 になることの確認、そして素朴な参照実装と高速実装の一致。最後の項目のために、BLAS 呼び出しに置き換えた後も三重ループの素朴な実装を削除せず、同じ入力に対して両者が $10^{-12}$ 以内で一致することを検査し続ける。速い実装しか残っていない状態は、比較の基準を失った状態である。

**第二層は統計的な検定。** 決定的に検査できないものだけをここに置く。中心は、事前分布からパラメータを引いてデータを生成し、そのデータで推論を走らせ、事後サンプルにおける真のパラメータの順位が一様分布することを確かめる手法である。広い範囲の誤りを検出するが、遅く、確率的で、誤りの箇所を特定しない。だから最後の網として置き、第一層で捕まえられるものを第二層に回さないことを規律とする。

**順序に注意。** 検証の計画が設計の後に来たのではない。乱数の方式も、微分の二段構成も、書き換え中心の方針そのものも、決定的なテストを成立させることを理由に選ばれている。検証の要求が設計を決めた、というのがこのプロジェクトの実態である。

そしてこの構成は実際に働いた。階層モデルで分散パラメータの被覆が悪かったとき、順位の検査が「実装のバグ」と「平均場近似の限界」を区別した。群数を 6 から 64 に増やすと被覆が改善したことで、近似の問題でありバグではないと確定した。

---

## 10. 既存の道具と研究との関係

各系譜が、それぞれ違う一点で止まっている。

### PyTorch

任意の Python コードの中で微分が使え、条件分岐もループも自由に書ける。実行された経路だけが記録されるので、柔軟さでは他を圧倒する。

**正確に言うと、構造は存在する。** 順伝播の最中に逆向きのリンクを張ったグラフが作られ、そのおかげで逆向きに辿れる。ただしグラフに入っているのは「逆伝播の関数をどの順で呼ぶか」であり、元の式の形ではない。そして各ノードは逆向き計算に必要な**数値**を保存しているため、そのグラフは特定の入力値に固定されている。入力が変われば作り直しである。

したがって差は「構造があるかないか」ではなく、**そのグラフが特定の入力値に固定されているかどうか**である。実行前に存在せず、実行するたびに作り直され、印字しても保存値の並びであって式ではない。

なお `torch.fx` を使えば順方向の式を構造として取り出せるので、「PyTorch は記述をデータとして持てない」という主張は成立しない。正確な差は、**既定の微分の経路では順方向の式が残らず、確率的な操作の書き換えと合成できない**という限定された形になる。

### JAX

関数を一度トレースして中間表現を取り出し、それに対して微分・ベクトル化・コンパイルを施す。微分は線形化と転置の二段に分かれている。乱数は鍵から値への決定的な関数である。

**微分に関しては、この設計と JAX の差はほとんどない。** 二段の構成は JAX から借りたものであり、借りられるほど同じである。値を記号のまま扱う点も同じで、§10 の PyTorch に対する指摘は JAX には当てはまらない。

差として残るのは三点だけである。第一に、JAX の式は Python をトレースして作るので、守るべき制約（純粋関数だけを書く、通常の条件分岐を使わない）が型でも構文でも強制されず、文書の注意書きとして存在する。この設計では言語仕様として拒否される。第二に、確率的な操作の書き換えは JAX の枠外であり、上に載る NumPyro は実行時のハンドラで実現しているため、書き換えた結果が式として残らない。第三に、中間表現への新しい操作を足せるのは JAX 開発者であり、利用者の拡張点ではない。

### Stan

モデル記述をコンパイルして対数密度関数を作り、成熟したサンプラで事後分布を得る。応用統計における事実上の標準であり、信頼性の実績は他のどれよりも厚い。

問題は三つ。密度が定義そのものであり、変数変換の補正項を利用者が手で書き足す場面がある。推論の手続きが固定されており、変分推論の目的関数を組み替える、検定統計量を同じモデルから導くといった用途は枠の外にある。微分は内部実装であり、利用者からは見えない。

**取り入れたもの**: 制約付きパラメータを変換して扱う発想。そして検証の文化——事前分布からデータを生成して推論を検査する手法は Stan 周辺で確立されたもので、この設計では統計的検査の層の中心に置いている。

### Pyro と NumPyro

モデルを普通の関数として書き、その中のサンプル操作を実行時に外から差し替える。差し替えの仕組みは少ないコードで新しい推論を追加でき、拡張性はこの系譜が最も高い。

問題は、読み替えた結果がプログラムとして存在しないことである。変分推論の目的関数は実行の中で組み上がって数になり、組み上がった式はどこにも残らない。符号を一つ間違えた目的関数はエラーを出さず、やや悪い結果を出すだけである。

**取り入れたもの**: 一つの記述を複数の目的で読むという中核の発想。離散変数の消去を配列の軸への割り当てとして行う手法（この設計の配列指向と特に相性が良い）。

### Gen とその周辺の研究

推論を利用者がプログラムできるものとして定式化した。モデルが備えるべき操作を最小のインタフェースとして規定し、その上に各種の推論を部品として組む。理論面の生産も厚い。

問題は、実装が Julia または Python への埋め込みであり、記述を完結したデータとして取り出せないこと、微分を外部に委ねていること。したがって理論が保証する性質と、実装が実際に検査できる性質の間に距離がある。

**取り入れたもの**: この系譜からが最も多い。モデルの操作を「生成する」「密度を測る」の対で捉える定式化。変数の集合を実行前に固定し型で検査する方針。そして採らない判断も一つ——密度を不偏推定量で代用する体系は、対数を取ると不偏性が壊れて変分推論と噛み合わず、決定的テストを全て統計的検定に落とすため採用しない。ただし後から追加できるよう、重みの扱いの規約だけ先に固定してある。

### 配列言語（APL・BQN）と Dex

APL 系からは、添字を書かずに配列全体を演算する体系、演算の適用次元を明示する規律、関数を値として持たない一階の伝統を受け継いでいる。逐次計算のための構成子も APL には元からあり、独立な軸と依存する軸が対になるのは配列言語として自然な設計である。

Dex は配列を添字上の関数と見る定式化を実装まで通した研究言語である。利用者が添字ループを書けるようにしたため、ループ内の逐次的な加算を微分する専用の仕組みを必要としている。この設計はループを外したので、その仕組みが問題ごと消えた。

### 数式処理

微分を式から式への書き換えとして実装した最も身近な例である。問題は式の膨張で、これが「書き換え方式は実用にならない」という通念の根拠になっている。

この設計では、書き換えの対象を数式ではなく代入文の列とし、共有部分を変数の束縛として保つ（展開しない）。加えて命令の粒度が粗いので、式の長さはプログラムの長さ程度にしかならない。

### 位置づけ

**個々の部品には新規性がほとんどない。** 二段の微分は JAX から、読み替えの発想は Pyro と Gen から、規律は配列言語から、定式化は Dex から、検査手法は Stan 周辺から借りている。

新しいのは組み合わせであり、それを一言で言えば、**理論が保証する等式（随伴の内積等式、書き換え前後の同値性）を、そのまま実行可能な決定的テストにする**という一点に向けて全部品を選び直したことである。

---

## 11. 現在地

実装は OCaml。テスト 250 本超。

| 段階 | 内容 | 状態 |
|---|---|---|
| 0–5 | 配列の見え方とその随伴、カーネル、構文木、評価器、次元適用の展開 | 完了 |
| 6–8 | 自動微分（線形化・分離・転置）、線形回帰の学習 | 完了 |
| 9 | BLAS の組み込みと手書き数字認識 | 完了 |
| 10–11 | 乱数、分布の代数、再パラメータ化、変分オートエンコーダ | 完了 |
| 12 | 階層モデル | 完了 |
| 13 | 求積、尤度比検定、ベイズファクター | 完了 |
| 14 | 逐次計算の構成子、拡散モデル、表層構文 | 進行中 |

### 主な結果

**多層パーセプトロン（段階 9）。** 手書き数字認識でテスト精度 96.97%、1 ステップ 11.9 ミリ秒。演算粒度を粗く保てば素朴な実行方式でも実用速度が出る、という仮説の検証。

**変分オートエンコーダ（段階 11）。** 784-400-20、5 エポックで検証集合の変分下界 $-107.0$、潜在次元は 20 本とも活性。10 個のパラメータについて有限差分と勾配が一度に一致。微分の戦略を指定する注釈が一つも不要だった。

**境界尤度比検定（段階 13）。** 帰無仮説下で 200 回のブートストラップ。理論が予測する分布（半分が原子、半分が自由度 1 のカイ二乗）に対し、原子質量 0.575（理論 0.5、$2.1\sigma$）、正の部分の分布距離 0.076（臨界値 0.148）。

**ベイズファクター（段階 13）。** 同じモデル記述から $\log \mathrm{BF}_{10} = 25.28$。閉じた式のある場合との照合が $6.1\times10^{-11}$。そして検定の統計量 54.0 とベイズファクターの 50.6 の差 3.4 が、情報量規準的な補正（$\log 64 = 4.16$）とオーダーで整合する。**頻度論とベイズが同じデータから独立に正しい答えを出している。**

**拡散モデル（段階 14）。** 二次元のらせん状データで学習し、逆過程で生成。学習は逐次構成子を使わず既存機能のみ、生成は逆向きの逐次計算一つ、乱数は全て外で引く。中間状態を保存する仕組みにより、ノイズから形が現れる過程が観察できる。

**分布の代数が実際に働いている。** 正規分布の対数密度に専用の分岐は存在せず、押し出しの一般規則から導出されている。その結果が閉じた式と $10^{-12}$ で一致することを検査している（§7）。

**式を保持して繰り返し評価できる。** 変換は一度だけ走り、生成された束縛列と本体を保持したまま、パラメータを含む環境だけを差し替えて再評価する。これが学習の反復を成立させており、同じ仕組みでサンプラも書ける。ハミルトニアン型のサンプラは既に動いている。

### 検証の要求が設計を決めた実例

段階 12 で階層モデルの分散パラメータの被覆が悪く出たとき、順位の検査が「実装のバグ」と「平均場近似の限界」を区別した。群数を増やすと被覆が改善したことで、近似の問題であると確定した。**この判別は、決定的な検査を先に全て通してあったからこそ意味を持つ。**

### 原理の少なさ

段階 12 以降、新しい原理をほとんど足していない。段階 12 では棄却による台の制限を作らずに済んだ（必要と思われた分布が全て「関数で送る」だけで書けた）。段階 13 は既存原理の適用のみ。段階 14 で加えた逐次計算も、ステップ数が静的に決まるので有界性の枠内である。

そして、別々の理由で選んだ判断——一括適用の軸、変数集合の固定、固定節点の求積、固定反復の最適化、逐次計算のステップ数——が、後から見るとすべて「作業量を静的に有界にする」という一つの原則に収束していた。

---

## 12. 失うものと、残っている問題

### 表現力として捨てたもの

要素単位のループ。実行のたびに確率変数の数が変わるモデル。関数を値として渡す抽象化。実行時にしか形が決まらない配列。モデルの中に書く、動的に停止する反復。

### ただし、推論アルゴリズムは別の層にある

上の制約は**モデル記述と、そこから生成される式**にかかるものである。推論アルゴリズムそのものには、かからない。

言語が提供するのは、任意の点で $\log p(\theta)$ とその勾配を評価する式である。それをどう使うかは、実装言語側のループの仕事になる。学習の反復（Adam）が既にそうであり、同じ場所にサンプラも書ける。

| 場所 | 何を書くか | 動的な制御 |
|---|---|---|
| モデル記述 | 確率変数と依存関係 | 不可 |
| 生成される式 | 密度・勾配・目的関数 | 不可 |
| **実装言語側** | **推論アルゴリズム、学習ループ** | **自由** |

したがって、勾配に基づくサンプラ——ランダムウォーク型、ランジュバン型、ハミルトニアン型、そして木の深さが動的に決まる適応型——は、いずれも書ける。実際、ハミルトニアン型は既に動いている。動的な木の構築は、モデルの外側にあるので制約を受けない。

この構成は他の系（Stan は C++、PyMC と NumPyro は Python、Gen は Julia）と同じであり、この設計に固有の制約ではない。ただし**「推論をプログラムする」という主張の適用範囲は、目的関数の構成までである**ことは明記しておくべきである。目的関数を組み立てる部分は言語の中で変換として書かれ、それを消費するアルゴリズムは外にある。

弱いのは成分ごとの更新である。一部の変数だけを変えたときに影響する項だけを再計算する機構がないため、素朴な成分別サンプラは全体の再評価を繰り返すことになる。ただし離散変数は列挙で消せるので、実用上の穴は限定的だと見ている。

### 透明性の限界

この文書は「式が読めるから検査できる」を繰り返し述べたが、**逐次計算の内部では成立しない**。100 ステップ目の値は 99 ステップ目に依存しており、「そこで何が起きたか」は式を読んでも分からない。生成が壊れたとき、式は正しくても途中で発散している、という事態は軌跡を見ないと分からない。

補償として、各ステップの状態を全て保存する仕組みを持たせた。透明性が消えたのではなく、**式を読んで納得する**から**軌跡を見て納得する**に形が変わった、という整理になる。

### 実用としての現状

**表層構文がない。** いまも実装言語のデータ構造としてプログラムを組んでいる。これは遅れではなく意図的な順序で、構文は最も変わりやすく最も安く後から追加できる部分である。実際、この設計の過程で意味論は複数回改訂され、混合分布の部品が消え、配列操作の語彙が総入れ替えになった。構文を先に作っていたらそのたびに書き直していた。

**速度が足りない。** 要素あたり約 20 ナノ秒、行列積で 32 GFLOP/s。変分オートエンコーダの規模までは実用的だが、大規模なモデルは学習できない。「書ける」ことの証明であって、既存フレームワークの代替ではない。

**他人が使える状態ではない。** 構文がなく、エラーメッセージも整備されておらず、書ける範囲が狭い。

### 判定

当初「価値が見合うかは検定まで行かないと判定できない」と書いたが、判定は出た。ただし出たのは**設計の正しさ**についてであり、**道具としての価値**ではない。

設計の側では、一つのモデル記述から最適化・ベイズ推論・検定が実際に出て、しかも新しい原理をほとんど足さずに拡散モデルまで届いた。当初の主張は裏付けられた。

道具の側は、構文・性能・文書という、設計とは別種の作業を残している。そして最も正直に言えば、明日論文を書くなら既存のツールを使うべきである。この言語が価値を持つのは、生成の途中経過を検査できることに高い値段を払う場合に限られる。
