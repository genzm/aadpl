# 配列・自動微分・確率を一つにした言語 — 設計と実装の解説

**想定読者**: 数理統計の研究者。測度論と推定論の言葉が通じ、普段は Python(numpy、場合によっては PyTorch や Stan)を使っている方。

**この文書の目的**: 何を作っているか、なぜその設計にしたか、正しさをどう保証しているかを説明する。実装言語(OCaml)のコードは出さない。

---

## 0. なぜ新しい言語を作るのか

### 0.1 現状で困ること

階層モデルを立てて、次のことを順にやりたいとする。

1. 最尤推定する(ランダム効果は積分して消す)
2. その分散成分がゼロという帰無仮説を尤度比検定する
3. 漸近分布が信用できないので、パラメトリックブートストラップで帰無分布をシミュレートする
4. 同じモデルでベイズファクターも計算して、頻度論の結論と比べる

現在これをやると、たいてい次のようになる。

- 1 は R の `lme4` か Python の `statsmodels`
- 3 は 1 をループで $N$ 回呼ぶ。$N=1000$、各フィットが 0.3 秒なら 5 分
- 4 は Stan か PyMC で**モデルを書き直す**
- 途中で「この共変量を対数変換したら」と思いつくと、**三箇所を直す**

問題は速度ではない。**同じモデルが三つの異なる記述で存在すること**である。三つの間に整合性の保証はなく、モデルを変更するたびに手で同期する。

一方、機械学習の側では別の状況がある。変分オートエンコーダを PyTorch で書くのは容易だが、その `Encoder` クラスを使って尤度比検定をしようとは誰も思わない。逆に Stan で書いた階層モデルにニューラルネットワークを埋め込むのも現実的でない。

**この言語の目的は、これらを一つの記述で扱えるようにすることである。** モデルは一度書く。最尤推定するか、事後分布を取るか、検定するかは、そのモデルに**どの操作を適用するか**の違いにすぎない、という形にしたい。

### 0.2 なぜ既存の言語の上に作らないのか

「Python のライブラリでよいのでは」という問いは正当である。実際、JAX の上には NumPyro があり、かなりのことができる。

しかし二つの点で足りない。

**第一に、繰り返しの扱いが言語の外にある。** Pyro や NumPyro には `plate` という構文があり、「この範囲の確率変数は独立に $N$ 個ある」ことを宣言する。同時に、JAX には `vmap` という変換があり、「この関数を $N$ 個のデータに一括適用する」ことを表す。この二つは**別の機構**として提供されている。しかし数学的には同じものである(後述 §2.2)。別々の機構であるために、`plate` の中で `vmap` を使うときの規則を人間が覚える必要がある。

**第二に、密度と分布の関係が言語に組み込まれていない。** 確率分布のライブラリは、各分布に `log_prob` メソッドを持たせる実装が普通である。これは「分布とは密度関数を持つオブジェクトである」という設計だが、測度論的にはそうではない。密度は**参照測度に対する Radon–Nikodym 微分**であり、参照測度を指定しなければ定義されない。この区別が曖昧だと、次のような場面で破綻する。

- ディリクレ分布の密度は何に対する密度か($J$ 次元ルベーグ測度ではない)
- 離散と連続が混ざった分布の「密度」とは何か
- 変数変換したときヤコビアンは誰が計算するのか

こうしたことは、既存ライブラリでは個別に対処されている。ヤコビアンは `TransformedDistribution` が持ち、単体は特別扱いされる。**個別対処が悪いのではなく、個別対処が必要になる設計が問題**である。

### 0.3 この言語が置く三つの原則

以上を踏まえて、次の三つを設計の出発点にした。

**(1) 分布は代数である。** 分布に属性(密度、台、変換可能性)を手で持たせない。少数の原始分布と少数の構成子から作り、属性は構成から**導出する**。これにより「新しい分布を足したら属性を書き忘れる」ことがなくなる。

**(2) 繰り返しは配列の軸である。** 「$N$ 個の独立な確率変数」と「$N$ 個のデータへの一括適用」を同じ機構で表す。これは配列言語の rank polymorphism という概念で、後で詳しく述べる。

**(3) 微分は変換である。** 自動微分をライブラリ関数ではなく、プログラムからプログラムへの変換として実装する。これにより「勾配が正しい」ことを、実行結果の照合ではなく**数学的な等式**で検証できる。

以下、これらが実際にどういう形になるかを見ていく。

---

## 1. 何が書けるようになるか

まず結果を見せる。以下は設計中の表層構文であり、記号は変わりうる(理由は §5.1)。**モデルの記述が一度だけである**ことに注目してほしい。

### 1.1 階層ロジスティック回帰

群 $g = 1,\dots,G$ があり、群ごとにランダム切片 $\alpha_g$ を持つロジスティック回帰。

$$
\alpha_g \sim \mathcal{N}(\mu, \tau), \qquad
y_{gi} \sim \mathrm{Bernoulli}\bigl(\mathrm{logit}^{-1}(\alpha_g + x_{gi}^\top\gamma)\bigr)
$$

```
Model ← {x‿y‿n:
  μ ?← Normal 0‿5
  τ ?← HalfNormal 1
  γ ?← Normal 0‿5
  α ?← Normal⎉0 μ‿τ ⍟ G
  η ← α +⌜ (x MatVec γ)
  ! (Mask n × LogBernoulli η‿y)
}
```

`?←` がサンプリング、`!` が観測(尤度への寄与)である。`⍟ G` は「$G$ 個」を意味する。

### 1.2 三つの使い方

同じ `Model` に対して、異なる操作を適用する。

**最尤推定**(ランダム効果 $\alpha$ を積分し、残りを最大化):

```
Fit ← Optimize 200 (Model _marginalize "α" _quadrature 21)
```

**尤度比検定**($\tau = 0$ という帰無仮説。境界にあるので $\chi^2$ の混合を使う):

```
lr ← 2 × (Fit1 data) - (Fit0 data)
p  ← 0.5 × 1 - Chi2CDF 1‿lr
```

**パラメトリックブートストラップ**(帰無分布を $N$ 回シミュレート):

```
reps ← (Simulate Model₀ θ̂₀)⍟N
lrs  ← LRT⎉1 reps
p    ← (+´ lrs > lr) ÷ N
```

**ベイズファクター**:

```
w1 ← (Model  _importance 1000) data
w0 ← (Model₀ _importance 1000) data
logBF ← (LogSumExp w1) - (LogSumExp w0)
```

### 1.3 変分オートエンコーダ

同じ言語で、機械学習側の例。

```
Guide ← {x:
  h ← Enc.L1 Dense x
  μ‿logσ ← Split (Enc.L2 Dense h)
  z ?← Normal μ‿(Exp logσ)
  z
}

Model ← {x:
  z ?← Normal 0‿1
  logits ← Dec.L2 Dense (Relu (Dec.L1 Dense z))
  ! LogBernoulli logits‿x
}

Step ← Adam ∇⟨Enc,Dec⟩ (Model ELBO Guide)
```

**構造が階層モデルと同じ**であることに注目してほしい。`?←` で潜在変数を引き、`!` で観測を与える。違いは、決定的な部分にニューラルネットワークが入っているかどうかだけである。

### 1.4 この節の主張

上の例が示したいのは、次のことである。

- モデルの記述は一つ
- 頻度論・ベイズ・変分推論は、そのモデルに適用する**操作**の違い
- 潜在変数を「積分する」「最大化する」「残してサンプリングする」の選択が、明示的に書かれる

最後の点は §3.3 で詳しく述べる。既存の道具では、この選択がモデル記述に埋め込まれてしまっている(Stan では潜在変数は必ず「残す」、`lme4` では必ず「積分する」)。

---

## 2. 三つの設計判断

### 2.1 分布は代数である

#### 何が問題か

確率分布ライブラリの標準的な設計は、各分布をクラスにして、密度・サンプリング・平均・分散などのメソッドを持たせるものである。この設計には次の問題がある。

新しい分布を追加するとき、**すべてのメソッドを正しく実装する責任が実装者にある**。たとえば切断正規分布を追加するなら、密度に正規化定数を入れ忘れてはいけないし、サンプリングは棄却法かもしれないし、再パラメータ化が可能かどうかも判断して属性に書く必要がある。実装者が一つ間違えると、その分布を使ったモデルは黙って間違った答えを出す。

さらに、分布の属性は互いに独立ではない。台が $(0,\infty)$ なら制約変換は対数であるべきで、密度の定義域もそれに従う。これらの整合性を人間が保証している。

#### どうしたか

**原始分布を二つに絞り、構成子を三つ用意した。**

原始分布:

$$
\mathrm{Uniform}[0,1), \qquad \mathrm{Categorical}(w)
$$

構成子:

$$
\begin{aligned}
&\texttt{Pushforward}(f, \mu) && f \text{ による押し出し。} f \text{ は像への微分同相} \\
&\texttt{Product}(\mu, \nu) && \text{独立積} \\
&\texttt{Restrict}(\mu, A, Z) && \text{事象 } A \text{ への制限。} Z = \mu(A)
\end{aligned}
$$

これだけである。正規分布は次のように作る。

$$
\mathcal{N}(0,1) = \texttt{Pushforward}(\Phi^{-1}, \mathrm{Uniform}), \qquad
\mathcal{N}(\mu,\sigma) = \texttt{Pushforward}(x \mapsto \mu + \sigma x, \mathcal{N}(0,1))
$$

対数正規分布は $\texttt{Pushforward}(\exp, \mathcal{N})$、半正規分布は $\texttt{Restrict}(\mathcal{N}(0,\sigma), x>0, 1/2)$、切断正規分布は $\texttt{Restrict}(\mathcal{N}, [a,b], \Phi(b)-\Phi(a))$ である。

#### なぜこれで十分か

属性がすべて構成から導出できる。

| 属性 | Prim | Pushforward | Product | Restrict |
|---|---|---|---|---|
| 台 | 定義から | $f(S_\mu)$ | $S_\mu \times S_\nu$ | $S_\mu \cap A$ |
| 参照測度 | 定義から | 保存される | 積測度 | 不変 |
| 密度 | 定義から | $p_\mu(f^{-1}y)\,\lvert\det Df^{-1}\rvert$ | 積 | $p_\mu \mathbf{1}_A / Z$ |
| 制約変換 | 台から決まる。$(0,\infty)$ なら $\log$、$(a,b)$ なら $\mathrm{logit}$ | | | |

**ヤコビアンは人間が書かない。** $\lvert\det Df^{-1}\rvert$ は $f^{-1}$ の式に自動微分を適用して得る。これは §2.3 の自動微分が確率の層でも働くということで、両者を同じ言語に入れた最初の利得である。

#### 何を諦めたか

混合分布を構成子にしなかった。理由は、混合が押し出しでは作れず、独立な構成子として足すと上の導出規則が全部壊れるからである。混合の台は区間でなくなり、成分ごとに参照測度が違いうる。

代わりに、**混合はプログラムとして書く**。

```
k ← ? Categorical w
? components‿k
```

そして周辺化が必要なら、「離散変数を列挙して重み付き和を取る」という変換を後から適用する。これは配列の軸を一本増やして総和を取る操作なので、§2.2 の枠組みにそのまま乗る。

#### 参照測度を明示することの帰結

密度は参照測度に対する Radon–Nikodym 微分としてのみ定義される、という原則を貫くと、いくつかのことが自動的に正しくなる。

ディリクレ分布を例に取る。$J$ 個の成分を持つが、単体は $\mathbb{R}^J$ の中の $J-1$ 次元部分多様体なので、$J$ 次元ルベーグ測度に対する密度は存在しない($\lambda_J$ 測度がゼロの集合に載っている)。

この言語では、そもそも $J$ 成分の値を持たせない。値の型は $J-1$ 次元とし、単体は $\mathbb{R}^{J-1}$ の開集合からの押し出しとして構成する。第 $J$ 成分は導出値であって確率変数ではない。

一般化すると、**台が値の型より低い次元の部分多様体になる場合、測度はチャート(座標系)からの押し出しとして構成し、値の型はチャートの次元を持つ**という規則になる。球面、正定値行列、直交行列も同じ規則で扱える。

そして「どのチャートを選ぶか」は仕様の一部である。同じ分布でも、単体上のルベーグ測度に対する密度と、球面に写した場合のハウスドルフ測度に対する密度は違う値になる。$\alpha_i = 1/2$ のディリクレ分布は球面上の一様分布に対応し、$\alpha = 1$ は単体上の一様分布に対応する——これは「密度は参照測度に相対的である」ことの具体例である。

### 2.2 繰り返しは配列の軸である

#### 何が問題か

確率モデルには「同じ構造が $N$ 個ある」という状況が頻出する。階層モデルの群、ベイズ推論のサンプル、ブートストラップの複製。機械学習では「バッチ」がこれにあたる。

Pyro はこれを `plate` という構文で扱う。

```python
with pyro.plate("groups", G):
    alpha = pyro.sample("alpha", dist.Normal(mu, tau))
```

`plate` は二つの仕事をしている。第一に、この範囲の確率変数が条件付き独立であることを宣言する。第二に、確率変数に名前空間を与える(`alpha[0]`, `alpha[1]`, …)。

一方 JAX には `vmap` がある。

```python
batched_f = jax.vmap(f)
```

これは「関数 `f` を $N$ 個の入力に一括適用する」変換で、内部ではバッチ次元を追加してプリミティブを呼び直す。

**この二つは同じものである**、というのがこの言語の出発点である。

#### 数学的に見ると

添字集合 $I$ 上の族への持ち上げを考える。関数 $F : A \to B$ に対し、

$$
F^{\otimes} : \prod_{i \in I} A \to \prod_{i \in I} B, \qquad
(F^{\otimes}a)_i = F(a_i)
$$

これが「繰り返し」の正体である。そして次の二つが成り立つ。

**微分と可換である。** 接空間を取る操作を $T$ と書くと、

$$
T(F^{\otimes}) = (TF)^{\otimes}
$$

つまり「$N$ 個に持ち上げてから微分する」と「微分してから $N$ 個に持ち上げる」が一致する。これが `vmap` が微分と交換できることの理由である。

**独立な積測度への持ち上げになる。** 各 $i$ で独立にサンプリングすることは、積測度からのサンプリングである。これが `plate` の意味である。

同じ持ち上げが、微分の側では `vmap`、確率の側では `plate` に見えている。**別の機構である必要はない。**

#### どうしたか

配列言語の rank polymorphism を採った。APL 系の言語(J、BQN)にある概念である。

記法 `F⎉k` は「配列の**末尾 $k$ 軸を一つの単位(セル)として** `F` を適用する」ことを表す。残りの先頭軸は繰り返しの軸(フレーム)になる。

例を挙げる。$x$ が形 $[B, 784]$ の配列、$W$ が形 $[784, 400]$ のとき、

```
x MatMul⎉2 W
```

は「末尾 2 軸(つまり $784$ と、$W$ の $[784,400]$ 全体)をセルとして行列積を取る」。$x$ のフレームは $[B]$、$W$ のフレームは空なので、$W$ は $B$ 回共有される。結果は $[B, 400]$ である。

`⎉0` なら要素ごと、`⎉1` ならベクトル単位、`⎉2` なら行列単位。**「何が一単位か」を書き手が宣言する**のがこの記法の要点である。

#### numpy の broadcast との違い

numpy は形を**末尾から**揃える。$[400]$ と $[B, 400]$ を足すと、末尾の $400$ が一致するので $B$ 方向に broadcast される。

この言語は**先頭から**揃える(leading agreement)。短いフレームは長いフレームの先頭部分でなければならない。

具体例で見る。バイアス $b$ が形 $[400]$、隠れ層 $h$ が形 $[B, 400]$ のとき、

```
b Add⎉0 h      → エラー
b Add⎉1 h      → 正しい
```

`⎉0` では両方の全軸がフレームになり、$[400]$ は $[B,400]$ の先頭部分ではないので不適合になる。`⎉1` では末尾 1 軸がセルなので、フレームは $[]$ と $[B]$ になり、前者は後者の先頭部分なので適合する。

**numpy なら黙って通る式が、この言語ではエラーになる。** これは欠点ではない。`⎉1` と書くことは「これはベクトルに対する演算で、$B$ は繰り返しの軸である」という宣言であり、numpy はその宣言を省略させる代わりに、意図しない整列を黙って通す。

実際、この規則は設計中に一度、筆者(この文書を書いている側)の誤りを検出した。バイアス加算の例を `⎉0` と書いたところ、実装が正しくエラーを出した。規則が正しく実装されていたので、仕様を書いた側の間違いが見つかった。

#### 実装での意味

`⎉` は変換として実装される。`F⎉k` を含むプログラムを、

1. 各引数のフレームを計算する
2. 先頭部分の一致を検査する(不一致ならエラー)
3. フレームが短い引数に、明示的な複製操作を挿入する
4. プリミティブの軸番号をフレームの分だけずらす

という手順で、`⎉` を含まない普通のプログラムに書き換える。

**3 の「明示的な複製」が重要**である。これを配列の実体コピーとして実装すると、$W$ を $B$ 倍の大きさに膨らませることになる。そうではなく、**同じデータを繰り返し読む「見え方」として実装する**(実装用語では stride 0 の view)。メモリは増えない。

そして後で述べる自動微分において、この複製の随伴(転置)は**フレーム方向の総和**になる。つまり「$B$ 個のデータから来た勾配を $W$ に合算する」という操作が、専用の規則なしに自動的に得られる。これが $T(F^{\otimes}) = (TF)^{\otimes}$ の実装上の帰結である。

### 2.3 微分は変換である

#### 何が問題か

自動微分の実装には大きく二つの流儀がある。

**演算子オーバーロード方式**: PyTorch のように、テンソルの演算を記録しながら実行し、逆向きに辿って勾配を計算する。実装は素直だが、「勾配が正しい」ことの検証は、有限差分との数値比較に頼ることになる。

**変換方式**: JAX のように、プログラムを受け取って勾配を計算するプログラムを返す。この言語もこちらを採る。

変換方式を採る理由は、**検証の質が違う**からである。有限差分との比較は、刻み幅の選び方に依存し、桁落ちの影響を受け、「たまたま合っている」を排除できない。一方、変換方式では次に述べる**厳密な等式**で検証できる。

#### 三つに分解する

逆モード自動微分を、単一のアルゴリズムとせず、三段階に分ける(Radul et al., POPL 2023 の定式化)。

$$
\text{前進モード微分} \;\longrightarrow\; \text{分離} \;\longrightarrow\; \text{転置}
$$

**第一段(前進モード微分)**: 各プリミティブに「値と方向微分の対を計算する」規則を与える。$\exp$ なら $(e^x,\ e^x\dot x)$、乗算なら $(xy,\ \dot xy + x\dot y)$。これは高校数学の微分公式をそのまま書いたものである。

**第二段(分離)**: 前段が生成したプログラムを、「値の計算」と「方向微分の計算」に分ける。ここで決定的なのは、**方向微分の部分が方向 $\dot x$ について線形である**ことである。上の例で、$\dot xy + x\dot y$ は $(\dot x, \dot y)$ の線形関数になっている($x, y$ は値の側で計算済みの定数)。

**第三段(転置)**: 線形な部分を逆向きにする。線形写像 $L$ に対し、その随伴 $L^*$ を求める操作である。

#### なぜこの分解が良いか

三つの利点がある。

**転置規則を書く対象が最小になる。** 非線形なプリミティブ($\exp$、$\log$、$\max$)には前進モードの規則だけ書けばよく、転置規則が必要なのは線形なプリミティブだけである。この言語では、転置規則は実質 5 系統しかない(配列の見え方の変更、添字による取り出し、総和、行列積、その随伴)。

**正しさを等式で検証できる。** 随伴の定義そのものである。

$$
\langle L(v),\, u\rangle = \langle v,\, L^{*}(u)\rangle
$$

$L$ が前進モード微分、$L^*$ が転置の結果である。乱数 $v, u$ を取ってこの等式を確かめれば、転置の実装の誤りはほぼ確実に捕まる。有限差分と違い、これは**刻み幅も打ち切り誤差もない厳密な等式**である。

この等式による検証は、実装の四つの層で使った(§4.1)。

**メモリの選択が自然に現れる。** 第二段で「この中間値を保存するか、逆向きのときに再計算するか」という選択が生じる。深層学習で checkpointing と呼ばれる技法は、この選択のことである。後付けの機能ではなく、分解の自由度として最初から表現される。

#### 微分可能性についての注意

自動微分の正しさを論じるとき、微妙な問題がある。$\mathrm{ReLU}$ や $\max$ は原点で微分不可能だが、実用上は「ほとんど至る所で微分可能だからよい」とされる。

しかし**ほとんど至る所で微分可能という性質は、合成について閉じていない**(Lee et al., NeurIPS 2020)。ほとんど至る所で微分可能な関数を合成した結果がそうでなくなる例があり、連鎖律の適用が存在しない微分の値を要求する場合がある。

正しいクラスは PAP(piecewise analytic under analytic partition)である。解析的な分割の上で区分的に解析的な関数のクラスで、合成について閉じており、**intensional derivative** と呼ばれる微分が至る所で定義され、連鎖律を満たし、ほとんど至る所で通常の微分と一致する。

この言語のプリミティブはすべて PAP クラスに属する。$\mathrm{ReLU}$ が原点で $0$ を返すこと、$\max(x,y)$ が同値のとき左を選ぶことは、恣意的な規約ではなく intensional derivative の計算として正当化される。

同じ枠組みは確率プログラムにも拡張されており(Huot et al., LICS 2023)、trace の密度関数がほとんど至る所で微分可能であること、密度計算に使える参照測度が存在することが示されている。後者は §2.1 で参照測度を割り当てた根拠である。

---

## 3. 確率をどう扱うか

### 3.1 分布とプログラムを層として分ける

区別が必要である。

**分布**(§2.1 の代数で作られるもの)は、密度が閉じた形で得られる。

**プログラム**(サンプリングの連鎖)は、出力の密度が一般には得られない。$z$ をサンプルして $y = f(z)$ を返すプログラムがあったとき、$y$ の密度は $f$ の逆像上の積分になり、閉じた形にならない。

しかし、**プログラムの trace(すべての確率変数の値の組)の上での密度は得られる**。各サンプリング地点の条件付き密度の積だからである。

この非対称性が、「サンプリング」と「観測」を基本操作にする理由である。

$$
\text{プログラムの意味} = (\text{trace 空間},\ p(t),\ w(t)), \qquad
\mu(dt) = w(t)\,p(t)\,\lambda(dt)
$$

$p$ は事前密度(各サンプリング地点の密度の積)、$w$ は観測による重み。観測は「重みに寄与を加える」操作として書かれ、別の基本操作を必要としない。

### 3.2 確率変数のアドレスと共通乱数

各サンプリング地点に**アドレス**を与える。アドレスは

$$
(\text{呼び出しの位置},\ \text{サイト名},\ \text{添字パス})
$$

の三つ組で、すべてプログラムの構文とフレームの添字から決まる。パラメータの値にも乱数の値にも依存しない。

添字パスがフレームに対応する。$G$ 個のランダム効果があれば、アドレスは $[1], [2], \dots, [G]$ の $G$ 個になる。**これが plate の実体である**——`plate` が名前空間とバッチ次元を同時に管理していたものが、この言語では `⎉` のフレームがそのまま担う。

乱数は次の形で引く。

$$
\texttt{random}(\text{実行キー},\ \text{アドレス})
$$

counter-based の擬似乱数生成器(Threefry)を使い、アドレスから直接乱数を計算する。逐次的な状態を持たないので、**評価の順序に依存しない**。$N$ 個のセルが並列に同じ式で乱数を引ける。

この設計の帰結が重要である。**パラメータ $\theta$ を変えても、同じアドレスからは同じ乱数が出る。** 確率空間 $\Omega$ を固定して確率変数を $X_\theta(\omega)$ と見ることに相当し、次の三つの場面で効く。

- **pathwise 微分の成立根拠**: $\theta$ で微分するとき、乱数は動かない
- **有限差分による検証**: $\theta$ を摂動しても乱数が変わらないので、比較が意味を持つ
- **分散削減**: 帰無分布と対立分布を同じ乱数で比較すれば、検出力の推定の分散が下がる

統計のシミュレーション研究では共通乱数は標準的な技法だが、この言語では**言語レベルで保証される**。

### 3.3 潜在変数の消し方は三つしかない

これが §1.2 で「操作の違い」と呼んだものの正体である。

潜在変数を扱う方法を数え上げると、次の三つになる。

$$
\begin{cases}
\text{積分する} & \text{(周辺化)} \\
\text{最大化する} & \text{(最適化)} \\
\text{残して重みをつける} & \text{(サンプリング)}
\end{cases}
$$

既存の手法を、この三つの割り当てとして表にできる。階層モデルで、潜在変数を $\alpha$(ランダム効果)と $(\mu,\tau,\gamma)$(母数)に分けると:

| 手法 | $\alpha$ | $(\mu,\tau,\gamma)$ | 事前分布 |
|---|---|---|---|
| 最尤推定 | 積分 | 最大化 | 使わない |
| MAP | 最大化 | 最大化 | 使う |
| ベイズ | 残す | 残す | 使う |
| 変分推論 | 残す(変分族) | 最大化(変分パラメータ) | 使う |
| 経験ベイズ | 積分 | 最大化 | 部分的に |

**モデルの記述は一つで、この割り当てだけが違う。** これが §1.2 の三つの使い方が同じ `Model` から出る理由である。

#### 順序が意味を持つこと

$\int$ と $\max$ は交換しない。

$$
\int \max \;\neq\; \max \int
$$

最尤推定は「$\alpha$ を積分してから母数を最大化する」であり、順序を逆にすると別の推定量になる。したがって、この割り当てを「各変数へのラベル付け」として表現することはできない。**入れ子の構造が要る。**

一方、積分どうしは交換する(測度に基づく意味論の可換性として証明されている)。非可換なのは最大化との組み合わせだけである。

#### 最大化を言語の構成子にしなかった理由

`Marginalize`(積分)と `Normalize`(事後分布の構成)は言語の構成子にした。しかし**最大化は構成子にしていない**。理由は三つある。

第一に、最大化は測度から測度を作る操作ではない。$\arg\max$ は測度論的な操作ではなく、「密度は測度に対する導出物」という原則と整合しない。

第二に、意味論に手続きが入り込む。実際の最適化は固定回数の反復であり、「その手続きの出力」という定義になる。宣言的な定義を持たない。

第三に、順序の問題が自動的に解決する。最適化を通常のプログラム(勾配と optimizer からなるループ)として書くと、`Marginalize` は最適化される関数の**内側に字句的に書かれる**。順序は入れ子構造がそのまま表現する。意味論に非可換性を書き込む必要がない。

この判断には実装上の副産物もあった。最適化ループを言語の式の中に展開すると、$M$ 回の反復に比例してプログラムが長くなる。$M = 100$、$N = 1000$ 複製のブートストラップでは、プログラムのサイズが現実的でなくなる。ループを言語の外に置くと、微分するプログラムは 1 ステップ分で済み、$N$ は配列の軸、$M$ は時間の反復として分離される。

### 3.4 周辺化の戦略

「何を周辺化できるか」は構成から決まる。「どう周辺化するか」は選択である。この分離は §2.1 の分布の設計、§3.2 の勾配の推定量の選択でも同じ形で現れる。

| 戦略 | 適用条件 | 保証 | 厳密な密度 |
|---|---|---|---|
| 厳密な列挙 | 台が有限 | 厳密 | 保つ |
| 共役性 | 構成が共役の形 | 厳密 | 保つ |
| 数値求積 | 低次元の連続変数 | 決定的だが偏りがある | **保つ** |
| モンテカルロ | 常に | 不偏 | 失う |

第三行が重要である。ガウス・エルミート求積で $\alpha$ を消したモデルは、**それ自体が厳密な密度を持つ、別の well-defined なモデル**である。決定的な関数なので最適化でき、後述する厳密なテストも通る。

$$
\begin{aligned}
\text{求積} &: \text{モデルを近似し、密度は厳密に保つ} \\
\text{モンテカルロ} &: \text{モデルを保ち、密度を近似する}
\end{aligned}
$$

一般化線形混合モデルの実務で求積が標準的なのは、この理由による。決定的な尤度が得られるから最適化できる。

そして、列挙と求積は**計算の形が同じ**である。どちらも「潜在変数を固定サイズの軸に置き換えて、重みつきで和を取る」操作であり、§2.2 の配列の枠組みにそのまま乗る。列挙は重みが 1、求積は重みが求積係数、という違いだけである。

### 3.5 作業量を有界にすること

実装は、潜在的に無制限な作業をすべて静的に有界にする。

| 箇所 | 有界化 |
|---|---|
| 棄却法による制限分布のサンプリング | 固定回数 $K$ の試行 |
| 最適化ループ | 固定回数 $M$ の反復 |
| 離散変数の周辺化 | 台のサイズ |
| 連続変数の周辺化 | 求積の節点数 $K$ |

四つとも同じ形である。有界にすることで、確率変数の個数が実行前に決まり、$N$ 個の複製を配列の軸として一括実行できる。

統計的にも損失はない。固定回数の反復による推定量は「その手続きの出力」として well-defined であり、すべての複製に同じ手続きを適用する限り、検定の妥当性は保たれる。むしろ手続きが完全に決定的になるので、再現性が上がる。

棄却法について補足する。$K$ 回の試行を「全部引いてから最初に受容されたものを選ぶ」形にすると、確率変数の個数が固定される。$K$ 回の試行は配列の演算一回になり、逐次ループにならない。全部棄却された場合は実行時エラーとする(失敗確率は $K$ で制御でき、ガンマ分布の標準的な棄却法なら受容率 0.98 程度なので $K=16$ で $10^{-27}$ 程度)。

そして、この形にすると**勾配が既存の機構から自動的に得られる**。「連続な変換 + 確率的な選択」なので、pathwise 微分と score function 推定量の合成になる。棄却法の勾配について手で導かれていた「再パラメータ化項 + 補正項」という分解(Naesseth et al., AISTATS 2017)が、特別扱いなしに出てくる。

---

## 4. 正しさをどう保証したか

ここが、この文書で最も伝えたい部分である。

確率的なプログラムは、間違っていても動く。損失が下がり、それらしい数字が出る。したがって「動いた」は正しさの証拠にならない。

### 4.1 随伴の等式を四つの層で使う

$L$ を線形写像、$L^*$ をその随伴とすると、定義から

$$
\langle L(v),\, u\rangle = \langle v,\, L^{*}(u)\rangle
$$

が任意の $v, u$ について成り立つ。逆に、乱数 $v, u$ でこの等式を確かめれば、$L^*$ の実装の誤りはほぼ確実に検出される。

この検査を、実装の四つの層で使った。

**第一層: 配列の見え方。** 配列の転置・部分取り出し・複製は、すべて「添字の写像 $\sigma$ による引き戻し」として表現される。その随伴は「$\sigma$ による押し出し」、つまり複数の位置から来た値の総和である。

$$
(\sigma_{*}g)(i) = \sum_{j \,:\, \sigma(j) = i} g(j)
$$

複製(同じ値を $N$ 回読む)の随伴が総和になるのは、この式で $\sigma$ が定値写像だからである。ランダムに生成した見え方の合成に対して、500 通りで等式を確認した。

**第二層: 数値カーネル。** 総和と複製が互いに随伴であること、添字による取り出しとその散布加算が互いに随伴であること、行列積の随伴が転置した行列との積であることを、それぞれ確認した。

**第三層: プログラム全体。** 前進モード微分と、三分解を経て得た逆モードの結果が、随伴の関係にあることを確認した。

**第四層: 確率プログラム。** 上と同じ検査を、サンプリングを含むプログラムに適用した。

同じ数学的性質を四回使ったことになる。検査のコードもほぼ流用できた。

### 4.2 独立な参照実装を必ず持つ

検査が循環しないようにする工夫が要る。

配列の見え方を検証するとき、最初は「実装した添字計算」と「別に書いた添字計算」を比較していた。しかし両者が同じ考え方(stride を使った計算)で書かれていると、**設計そのものが間違っていても両方が同じように間違う**。

そこで、stride を一切使わない独立な参照実装を書いた。「複製とは添字を一つ落とすこと」「転置とは添字を並べ替えること」という定義をそのまま関数にしたもので、効率は考えていない。これと実装を突き合わせることで、循環を断った。

同じ方針を三箇所で採った。

- 配列の見え方: stride を使わない参照実装
- 数値カーネル: 素朴なループ実装(高速な実装を入れた後も残し、両者の一致を検査し続ける)
- 自動微分: 前進モードの素朴なインタプリタ(変換方式の実装を検証する基準)

**参照実装は捨てない。** 速度のために消したくなるが、消すと「速い実装が正しいか」を確かめる手段がなくなる。

### 4.3 検査の被覆を組み合わせで測る

乱数で入力を生成する検査(property-based testing)には落とし穴がある。生成される入力が偏っていると、通っても何も証明していない。

配列の見え方の検査で、生成される見え方を分類して数えた。

- 複製(stride 0)を含むか
- 負の方向の部分取り出しを含むか
- 開始位置がずれているか
- 3 軸以上か

**単独の割合を見ても穴は見えない。** それぞれ 30% ずつ出ていても、独立なら三つ同時に満たすのは 3% 未満かもしれない。組み合わせの表を作り、27 通りのセルすべてに十分な数が入ることを確認して初めて、検査が意味を持つと言える。

実際、最初の生成器は複製を一度も生成していなかった。この場合、「複製の随伴が総和になる」という最も重要な性質が一度も検査されていないことになる。

### 4.4 評価は一致するが構造が壊れている場合

これが最も気づきにくい種類の誤りである。

自動微分の第二段(分離)で、プログラムの各行を「値の計算」と「方向微分の計算」に分類する。分類が間違っていても、**両方を評価すれば正しい答えが出る**。誤りが現れるのは第三段(転置)で、そこで初めて「非線形な演算が線形の側に混ざっている」ことが問題になる。

したがって、値の比較だけでは検出できない。**分類そのものを構文として検査する**必要がある。

具体的には、方向微分の側に現れる演算が線形なものだけであることを検査する。乗算なら「片側だけが方向微分に依存する」、除算なら「分子だけ」、非線形な演算($\exp$、$\log$、$\max$)なら「方向微分に依存する引数を取らない」。

この検査は、前進モード微分の出力が「証明可能に線形である」という性質(Radul et al. が線形型システムで保証しているもの)の、実行時版である。

### 4.5 統計的な検査

以上は決定的な検査で、厳密な一致を要求できる。密度を厳密に計算すること、共通乱数を使うことを選んだ結果、これらが**統計的検定ではなく単体テストとして書ける**。これが両者を選んだ主な実利である。

その上で、統計的な検査も必要になる。**Simulation-Based Calibration**(Talts et al.)を基盤に据える。

事前分布から $\theta$ を引き、その $\theta$ でデータを生成し、そのデータを条件として推論を走らせる。事後サンプルが正しければ、事前サンプル $\theta$ の順位統計量が一様分布する。多数の試行で順位の分布が一様でなければ、推論に問題がある。

これで次のものがまとめて検証できる。

- 制限分布の正規化定数 $Z$ が正しいか
- 勾配推定量が不偏か
- 重要度重みが正しいか

そして重要な点として、**SBC の計算の形は、統計研究者が普段やるシミュレーション研究と同じ**である。

$$
\underbrace{N \text{ 回の複製}}_{\text{配列の軸}} \times \underbrace{M \text{ 回の反復}}_{\text{ループ}} \times \underbrace{K \text{ 個の節点}}_{\text{配列の軸}}
$$

パラメトリックブートストラップ、検出力計算、シミュレーション研究、SBC——全部この形である。診断のための仕組みが、そのまま利用者向けの機能になる。

順序としては、決定的な検査を先に通し、統計的な検査を後に置く。決定的に検査できるものを統計的検定に委ねない。

---

## 5. 実装の現状と、まだできないこと

### 5.1 現状

決定的な部分(配列、自動微分、性能)は完成している。

- 配列の見え方とその随伴、数値カーネル
- 抽象構文木と評価器
- rank の展開
- 自動微分の三分解
- 線形回帰の学習(閉じた形の解と一致することを確認)
- BLAS の組み込みと MNIST での多層パーセプトロン(テスト精度 97%、1 エポック約 11 秒)

確率の部分は進行中である。

- 乱数生成、分布の代数、trace、サンプリングと密度計算: 完了
- 再パラメータ化と変分オートエンコーダ: 進行中
- 制限分布と階層モデル: これから
- 求積と検定: これから

### 5.2 表層構文がまだない

**現在、プログラムは実装言語(OCaml)のデータ構造として組み立てている。** §1 に示した記法は設計中のもので、パーサはまだ書いていない。

これは遅れではなく、意図的な順序である。構文は最も変わりやすく、最も安く後から追加できる部分だからである。実際、この設計を進める過程で意味論は四回改訂され、混合分布の構成子が消え、制限の構成子が入り、配列操作の語彙が総入れ替えになった。構文を先に作っていたら、そのたびに書き直していた。

構文を設計する際の未決事項が一つある。微分を表す記号として自然な `∇` は、参考にしている配列言語 BQN では自己再帰を意味する記号として既に使われている。別の記号を割り当てるか、記号でなく名前を使うかを決める必要がある。

### 5.3 現在の設計では扱えないもの

正直に列挙する。

**確率変数の個数が実行時に変わるプログラム。** 意味論としては定義してある(trace 空間が非交和になる)が、実装は固定構造に限定している。逸脱した場合はエラーとして拒否し、黙って近似することはしない。

**モンテカルロによる周辺化。** §3.4 の表の第四行。これを入れると厳密な密度が失われ、§4.5 の決定的な検査が全部統計的検定に落ちる。周辺化を上三つの戦略に限れば、この問題は生じない。

**再帰的な確率プログラム。** 非再帰のみ。

**関数を値として扱うこと。** この言語は一階である。関数を配列やレコードに入れることはできない。この制限には利点があり、測度が構文木として表現されるので、印字して読むことができ、再び入力できる。また、意味論の基盤が軽くなる(標準ボレル空間と s-finite kernel で足り、高階の場合に必要な quasi-Borel space が不要になる)。

**自然勾配。** 測度の空間の接空間をどう定義するかが未決である。ただし通常の変分推論には不要で、必要になるのは自然勾配法を使う場合のみである。指数型分布族と認識できれば標準的な公式が使えるので、構成をパターンマッチする書き換え規則として実現できる見通しがある。

### 5.4 採用しなかった選択肢

設計の過程で検討して採らなかったものを、理由と再考の条件とともに記録している。ここでは統計研究者に関係の深いものを二つ挙げる。

**混合分布の構成子**(§2.1)。押し出しでは作れず、独立に足すと導出規則が壊れる。混合はプログラムとして書き、周辺化は列挙の変換で行う。

**密度の不偏推定**(Stochastic Probabilities, Lew et al. 2023)。密度を厳密に計算せず不偏推定に置き換えると、周辺化や入れ子の推論を第一級に扱えるようになる。採らなかった理由は、**不偏性が保証されるのは密度であって対数密度ではない**ことである。$\mathbb{E}[\log\hat p] \neq \log\mathbb{E}[\hat p]$ なので、変分下界のように重みの対数を取る目的関数では不偏性が壊れる。重要度サンプリングや逐次モンテカルロは助けるが、変分推論は助けない。

ベイズファクターについては、重みの比なので対数を取る必要がなく、不偏推定量の比として正しく計算できる。したがって重要度重みを配列として露出させる設計(正規化定数の推定値ではなく重みそのものを返す)にしておけば、この機構なしで済む。

---

## 付録A: 実装の段階

参考までに、実装の進行順を示す。決定的な部分を先に完成させ、その上に確率を載せる順序になっている。

| 段階 | 内容 | 状態 |
|---|---|---|
| 0–2 | 配列の見え方とその随伴 | 完了 |
| 3 | 数値カーネル(素朴な実装) | 完了 |
| 4 | 抽象構文木と評価器 | 完了 |
| 5 | rank の展開 | 完了 |
| 6–7 | 自動微分(前進モード、分離、転置) | 完了 |
| 8 | 線形回帰の学習 | 完了 |
| 9 | BLAS の組み込みと MNIST | 完了 |
| 10 | 乱数、分布の代数、trace | 完了 |
| 11 | 再パラメータ化と変分オートエンコーダ | 進行中 |
| 12 | 制限分布と階層モデル | 未 |
| 13 | 求積と検定 | 未 |

段階 9 で検証したのは「配列演算の粒度を大きく保てば、素朴な実装でも実用速度が出る」という仮説である。rank の展開が意図通りに働けば、一回の演算が扱う要素数が十分大きくなり、インタプリタの固定費が相対的に無視できる。実測では、演算あたりの固定費が約 0.5 マイクロ秒、MNIST の 1 ステップが約 12 ミリ秒で、固定費の寄与は 0.1% 未満だった。

## 付録B: 参考文献

**微分**

- Lee, Yu, Rival, Yang. *On Correctness of Automatic Differentiation for Non-Differentiable Functions.* NeurIPS 2020.
- Huot, Lew, Mansinghka, Staton. *ωPAP Spaces: Reasoning Denotationally About Higher-Order, Recursive Probabilistic and Differentiable Programs.* LICS 2023.
- Radul, Paszke, Frostig, Johnson, Maclaurin. *You Only Linearize Once: Tangents Transpose to Gradients.* POPL 2023.

**確率プログラミング**

- Lew, Cusumano-Towner, Sherman, Carbin, Mansinghka. *Trace Types and Denotational Semantics for Sound Programmable Inference.* POPL 2020.
- Obermeyer et al. *Tensor Variable Elimination for Plated Factor Graphs.* ICML 2019.
- Naesseth, Ruiz, Linderman, Blei. *Reparameterization Gradients through Acceptance-Rejection Sampling Algorithms.* AISTATS 2017.
- Figurnov, Mohamed, Mnih. *Implicit Reparameterization Gradients.* NeurIPS 2018.
- Lew, Ghavamizadeh, Rinard, Mansinghka. *Probabilistic Programming with Stochastic Probabilities.* PLDI 2023.

**検証**

- Talts, Betancourt, Simpson, Vehtari, Gelman. *Validating Bayesian Inference Algorithms with Simulation-Based Calibration.* 2018.
