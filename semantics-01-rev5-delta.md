# 意味論 第1枚 rev5：差分改訂 — 逐次軸（Scan）の導入

**状態**: 第1稿
**位置づけ**: rev4 への差分改訂。rev4 の本文はこの文書が明示的に変更する箇所を除き、そのまま効力を持つ。
**改訂の動機**: 時間依存を持つ計算（拡散モデルの逆過程・自己回帰生成・HMM・固定反復 HMC）を言語の射程に入れる。rev4 はこれらを表現できず、しかも「できないこと」の記録にも挙げていなかった。本改訂は、その穴を **Scan（有界な逐次軸）** で埋め、同時に rev4 の三つの条文を書き換える。

**Phase 0–13 の完了を受けた検証状況の更新も含む（§R5-6）。**

---

## R5-0. 改訂の原則

rev4 までの改訂と同じ規律に従う。

1. **条文を運用で曲げない。曲げるなら改訂として明示する。** Scan は rev4 の二つの条文と字面上衝突する。黙って通すのではなく、条文自体を書き換え、書き換えの理由を残す。
2. **採用しなかった選択肢と再考条件を記録する**（rev4 §10 の方式）。本改訂では「Scan 本体内の確率的サンプリング」を封印し、再考条件を付す。
3. **すべての新しい主張は検証可能な形に落とす**（内積等式・閉形式照合・ビット一致）。Scan の検証手段を §R5-5 に列挙する。

---

## R5-1. 軸の二種類（新設 — rev4 の「複製は rank」の相対化）

### 旧条文（rev4）

> 複製は rank である。$N$ 回の反復・$G$ 個の群・$K$ 個の節点は、すべて配列の軸として表現され、入れ子の推論は入れ子ではなく軸である。

### 新条文

計算の反復には二種類あり、言語はそれぞれに一つの構成子を与える。

$$
\text{反復} \;=\;
\underbrace{\text{独立な複製}}_{\texttt{Rank}\ (\text{軸})}
\;\uplus\;
\underbrace{\text{依存する逐次}}_{\texttt{Scan}\ (\text{有界な再帰})}
$$

| | Rank | Scan |
|---|---|---|
| 反復間の依存 | なし（各セルは独立） | あり（$z_t$ は $z_{t-1}$ に依存） |
| 実行 | 並列（ベクトル化） | 時間方向に逐次。**各ステップの内部は Rank により並列** |
| 例 | バッチ、群、MC 複製、節点、チェーン、粒子 | 拡散逆過程、自己回帰生成、HMM の forward、leapfrog |

**規範**: Rank で書けるものを Scan で書いてはならない。Scan は反復間に真の依存があるときにのみ使う。この規範は静的に検査できないが（依存の有無は意味的性質）、設計規約として明記する。違反は誤りではなく劣化である — Scan で書いた独立反復は正しい値を返すが、並列性と透明性（§R5-3）を不必要に失う。

**旧条文が過大だった点の記録**: rev4 の「入れ子推論は入れ子ではなく軸である」は、時間依存のあるモデルが元から表現不能だったことを言わずに成立していた。SBC の $N=512$、ブートストラップの $B=200$、求積の $K$ 節点、HMC のチェーン — これらが軸で書けるのは**独立だから**であり、主張の正しい範囲は「独立な反復は軸である」だった。Scan の導入は主張の弱化ではなく、暗黙の適用範囲の明示である。

### Scan の定義

$$
\texttt{Scan}\{T;\ \text{carries};\ \text{inputs};\ \text{collect};\ \text{reverse}\}\ \texttt{in}\ k
$$

- $T$: **静的な**反復数。式ではない（有界作業量の原則 rev4 §7 に従う）
- carries: 持ち回り束縛 $[(z_1,e_1^0,e_1^{\mathrm{next}});\ldots;(z_m,e_m^0,e_m^{\mathrm{next}})]$。next 式は旧 carry と当該ステップの input cell を読み、すべて同時に更新される
- inputs: $[(x_1,xs_1);\ldots;(x_n,xs_n)]$。各 $xs_i$ は先頭軸長 $T$ を持ち、各ステップでその cell が $x_i$ に束縛される
- collect: 偽なら各 $z_i$ を最終 carry に、真なら更新後の carry 列 $[z_i^1;\ldots;z_i^T]$ に束縛する
- reverse: 真なら input cell を $T-1,\ldots,0$ の順で読む。collect の軸も元の添字順でなく**実行順**であるため、reverse input に対する forward Scan とビット一致する
- continuation $k$: Scan が束縛した carry 名を自由変数として持つ式。**Scan 式全体の値は $k$ の単一テンソル**である

Scan は値を複数返す構成子ではなく、`Let` と同型の**複数束縛構成子**である。これにより式の型（1 式 = 1 テンソル）を変えず、複数 carry と、微分変換が追加する residual 軌跡を同じ機構で表現する。next 式と continuation は構文的位置であり値ではないので、**一階性（rev4 §2.4）は保たれる。**

---

## R5-2. データフローグラフ条文の書き換え

### 旧条文（rev4 の含意）

> すべてのプログラムは、固定サイズの配列演算のデータフローグラフに落ちる。

### 新条文

> すべてのプログラムは、**有限展開のもとでデータフローグラフに等しい**。`Rank` と `Scan` は展開せずに構造として保持される。意味は展開形（有限の DAG）で定義され、実装は構造を保つ。

**理由**: この形は rev4 で既に `Rank` が採っていた。`Rank(k, p, \text{args})` は「$G$ 個のセルで評価せよ」という構造であり、`expand_rank` が展開する。Scan との違いは一点のみ — Rank は展開する変換を持ち（要素ごとの融合と形状確定のため）、Scan は持たない（$T$ 倍の AST サイズを避けるため）。**「意味は有限展開で定義し、実装が構造を保つ」という原理は両者に共通**であり、本改訂はこの原理を条文に昇格させる。

**帰結**: Scan の意味論は展開形で与えられる。すなわち

$$
\llbracket \texttt{Scan}\{T; z_0; xs; f\} \rrbracket
\;=\;
\llbracket \texttt{let}\ z_1 = f(z_0, xs_1)\ \texttt{in}\ \ldots\ \texttt{let}\ z_T = f(z_{T-1}, xs_T)\ \texttt{in}\ z_T \rrbracket
$$

であり、検証もこの等式に対して行う（§R5-5 の $\Inv$-1）。

---

## R5-3. 実行の透明性の等級（新設 — 「すべての中間物が印字できる」の精密化）

rev4 は「すべての中間物が印字できる」を思想として持つが、印字可能性には二つの水準があることを Scan が顕在化させた。

| 水準 | 内容 | Rank | Scan |
|---|---|---|---|
| **構造の印字** | プログラムが式として存在し、印字して読める | ✓ | ✓（body は一つの式。$T$ 個のステップは同じ式の反復であり、Rank の $G$ 個のセルが同じ式であることと対称） |
| **実行の透明性** | 式を読めば各部分の挙動が説明できる | ✓（どのセルも同じ説明） | **△** |

Scan では $z_t$ の値が $z_{t-1}$ に依存するため、「$t=500$ で何が起きたか」は式からは読めず、**軌跡の観察による**。collect がその手段であり、`Scan` の collect は透明性の補償機構として位置づけられる（デバッグ用の便宜ではなく、思想の一部である）。

**規定**: Scan を含むプログラムの診断は、collect した軌跡に対して行う。「式を読んで納得する」の Scan における対応物は「軌跡を見て納得する」である。

---

## R5-4. Scan と確率の分離（最重要 — 封印と再考条件）

### 規定

**Scan の body は `Sample` / `Score` を含んではならない**（変換の事前条件として強制。違反は loc 付きエラー）。

確率的な逐次過程は、乱数を Scan の**外**で先頭軸 $[T,\ldots]$ 付きの通常の `Sample` として引き、input で渡すことで表現する:

$$
x_{t-1} = \mu_\theta(x_t) + \sigma_t z_t
\quad\Longrightarrow\quad
z \sim \mathcal N(0,I)^{\otimes T} \ (\text{Scan の外})\,;\quad
\texttt{Scan}\ \text{の body は決定的}
$$

これは reparam の構造（乱数は外、本体は決定的 — 第2枚 §2.2 の `elim_samples` と同じ分解）の Scan への適用である。

### この規定が守るもの

trace 型の静性（rev4 §6.2）。body 内に `Sample` を許すと、trace が時間添字を持ち、サイトのアドレス集合が「どのステップか」に依存する。`check_trace_compat` は時間軸での対応を取らねばならず、`draw_noise` の counter 符号化に時間次元が入り、Phase 10–13 で構築した trace 機構の前提が全て動く。**Scan の導入と trace 型の拡張は別の設計問題であり、前者のついでに後者を滑り込ませてはならない。**

### この規定の下で書けるもの・書けないもの

| 書ける | 書けない |
|---|---|
| 拡散モデルの生成（乱数は外で全ステップ分引く） | — |
| 自己回帰生成（一様乱数を外で引き、inverse-CDF を body 内で決定的に計算） | — |
| HMM の forward / Viterbi（そもそも決定的な再帰） | HMM の潜在系列そのものを trace に持つ推論 |
| 粒子フィルタの重み更新（`_importance` の枠） | 粒子の trace を時間サイトとして扱う SMC の一般形 |
| 固定反復 HMC（受容判定は Step で決定的に書く） | — |
| カルマンフィルタ | 時系列潜在変数の変分推論（q が時間サイトを持つ） |

### 再考条件（rev4 §10 の方式で封印）

**「時系列の潜在変数に対する trace ベースの推論」が必要になったとき** — すなわち $q(z_{1:T} \mid y_{1:T})$ の各 $z_t$ が独立にサイトであるような変分族、または SMC の一般形を言語内で書く必要が生じたとき — に、**trace 型への時間軸の導入を独立の設計問題として**再検討する。その際の論点は少なくとも: (i) site 表の frame と時間添字の関係、(ii) counter 符号化への時間次元の追加、(iii) `check_trace_compat` の時間軸対応、(iv) 部分観測（一部の $t$ のみ観測）の表現。本改訂ではこれらを一切先取りしない。

---

## R5-5. Scan の検証手段（第2枚への委譲事項の予告）

Scan に対する $(\Pr, \Post, \Inv)$ の完全な記述は第2枚の改訂で行う。意味論として要求する不変条件のみここに列挙する。

| 不変条件 | 検査 |
|---|---|
| **$\Inv$-1 展開等価** | $\texttt{eval}(\texttt{Scan}) = $ ホスト言語で $T$ 回 eval したものと**ビット一致**（§R5-2 の意味論の直接検証） |
| $\Inv$-2 累積和の閉形式 | $\times$` の scan（累積積）等が閉形式と一致 |
| $\Inv$-3 内積等式 | Scan を含むプログラムの $\langle L(v), u\rangle = \langle v, L^*(u)\rangle$（微分対応後） |
| $\Inv$-4 JVP の正しさ | 線形 Scan の JVP を閉形式と、一般の Scan を FD と照合 |
| $\Inv$-5 reverse の整合 | reverse Scan = input を逆順にした forward Scan とビット一致 |

微分（$\Inv$-3, 4）は段階導入とする: 第一段階（Scan₁）では `grad` の入口で Scan を **loc 付きエラーとして拒否**し（rev4 §0「覆っていない部分はエラーとして拒否する」の適用）、第二段階（Scan₂）で転置規則（body の転置を逆順に scan する）を与える。**Scan₂ においても §R5-4 の規定は維持される。**

---

## R5-6. 検証状況の更新（rev4 の同名節を置換）

rev4 の「検証状況: MNIST 上の VAE と、階層ロジスティック回帰を**紙の上で**構成済み」を以下に置換する。

**Phase 0–13 完了（実装検証済み）:**

- **北極星①**: MNIST VAE（784-400-20、動的二値化、holdout ELBO −107.0、事前生成が読める、活性潜在 20/20）。戦略注釈ゼロで pathwise が構成から選択された（§5.5 の実証）
- **北極星②**: 境界尤度比検定。$2\log\Lambda$ の帰無分布が $\tfrac12\chi^2_0 + \tfrac12\chi^2_1$（$B=200$、原子質量 0.575、正成分の KS 距離 0.076）
- **北極星③**: ベイズファクター。同一の GLMM 記述から slot のロール（Maximize/Condition）の切替のみで $\log\mathrm{BF}_{10} = 25.28$。②との整合: $2\log\Lambda = 54.0$ vs $2\log\mathrm{BF} = 50.6$
- trace 型の四処理（**条件づける／積分する／最大化する／残す**）がすべて実装された。rev4 の「三処理」は observed サイト（条件づけ）を暗黙に含んでおり、Phase 12-3 で第四として明示化された
- `Restrict` は実装せずに Phase 13 まで完了した（HalfNormal・切断は $\Phi^{-1}$ 経由の Pushforward で構成）。代数の閉包の主張（Gamma/Beta/Dirichlet への到達）は**未検証のまま**であり、再考条件は rev4 §10 の記録の通り
- 有界作業量の原則は一度も破られなかった。棄却の $K$ は不要になり、求積の節点・列挙の台・固定反復・SBC の複製がすべて軸として実行された

**本改訂（rev5）時点で未検証:**

- Scan のすべて（本改訂は仕様先行である。§R5-5 の $\Inv$ が検収基準）
- 印字と round-trip（設計文書 18 章。表層構文とともに保留中）

---

## R5-7. 「できないこと」の更新

rev4 の「できないこと」の表を以下のように改訂する。

**表から削除される（Scan により可能になる）:**

- ~~時間軸を持つモデル全般~~ → 決定的な逐次過程・生成・フィルタリングは Scan の射程（§R5-4 の表）

**表に残る:**

- NUTS（軌道長がデータ依存 — 静的有界性）
- 次元が変わる推論（RJMCMC、open-universe — trace 型の静性）
- 収束判定で止まる反復（静的有界性）
- 可変長の状態（KV キャッシュの動的確保を含む — 静的形状）
- 打ち切りなしのノンパラメトリックベイズ

**表に追加される（Scan が持ち込む新しい限界の明示）:**

- **Scan を通した微分の不能な場合**: body に `Step` / `Argmax` 等の微分不能プリミティブを含む Scan（例: MH の受容判定、自己回帰の離散選択）は、値としては計算できるが `grad` の対象にできない。微分可能性は言語全体で一様ではなく、これは Scan 固有ではなく rev4 から存在した性質（`Step` は当初からある）だが、Scan により実用上顕在化するためここに明記する
- **時系列潜在変数の trace ベース推論**（§R5-4 の封印。再考条件付き）

---

## R5-8. BQN との整合（記録）

APL/BQN の伝統において scan（`` ` ``）と fold（`˝`）は for ループの代替として正統な語彙であり、本改訂は配列言語の思想からの逸脱ではない。`⎉`（独立な軸への適用）と `` ` ``（依存する軸の畳み込み）が対をなす構造は、§R5-1 の二分法の記法上の対応物として、表層構文の設計時にそのまま使える。

---

## 付録: 本改訂で書き換えられた条文の対照表

| rev4 | rev5 |
|---|---|
| 複製は rank である | 独立な複製は Rank、依存する逐次は Scan（§R5-1） |
| プログラムはデータフローグラフである | プログラムは有限展開でデータフローグラフに等しい。Rank と Scan は構造として保持（§R5-2） |
| すべての中間物が印字できる | 構造の印字は全構成子で成立。実行の透明性は Scan では軌跡の観察による（§R5-3） |
| 潜在変数の三処理 | 四処理（条件づける／積分する／最大化する／残す）（§R5-6） |
| 検証状況: 紙上構成 | Phase 0–13 実装検証済み。北極星①②③達成（§R5-6） |
